/*
 * Swaggy Jenkins
 *
 * Jenkins API clients generated from Swagger / Open API specification
 *
 * The version of the OpenAPI document: 3.4.1-pre.0
 *
 * Generated by: https://openapi-generator.tech
 */

//! API routing module
//!
//! This module defines the routing configuration for all API endpoints.

use salvo::prelude::*;
use crate::handlers::*;

/// Create the main router with all API routes
pub fn create_router() -> Router {
    let mut router = Router::new();

    // Routes for /api/json
    router = router.push(
        Router::with_path("/api/json")
            .push(
                Router::new()
                    .get(get_jenkins)
            )
            .push(
                Router::new()
                    .head(head_jenkins)
            )
    );
    // Routes for /blue/rest/classes/
    router = router.push(
        Router::with_path("/blue/rest/classes/")
            .push(
                Router::new()
                    .get(search_classes)
            )
    );
    // Routes for /blue/rest/classes/{class}
    router = router.push(
        Router::with_path("/blue/rest/classes/{class}")
            .push(
                Router::new()
                    .get(get_classes)
            )
    );
    // Routes for /blue/rest/organizations/
    router = router.push(
        Router::with_path("/blue/rest/organizations/")
            .push(
                Router::new()
                    .get(get_organisations)
            )
    );
    // Routes for /blue/rest/organizations/{organization}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}")
            .push(
                Router::new()
                    .get(get_organisation)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/")
            .push(
                Router::new()
                    .get(get_pipelines)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{folder}/
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{folder}/")
            .push(
                Router::new()
                    .get(get_pipeline_folder)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{folder}/pipelines/{pipeline}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{folder}/pipelines/{pipeline}")
            .push(
                Router::new()
                    .get(get_pipeline_folder_pipeline)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}")
            .push(
                Router::new()
                    .get(get_pipeline)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/activities
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/activities")
            .push(
                Router::new()
                    .get(get_pipeline_activities)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/branches
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches")
            .push(
                Router::new()
                    .get(get_pipeline_branches)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}/
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}/")
            .push(
                Router::new()
                    .get(get_pipeline_branch)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}/runs/{run}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}/runs/{run}")
            .push(
                Router::new()
                    .get(get_pipeline_branch_run)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/favorite
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/favorite")
            .push(
                Router::new()
                    .put(put_pipeline_favorite)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/queue
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/queue")
            .push(
                Router::new()
                    .get(get_pipeline_queue)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/queue/{queue}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/queue/{queue}")
            .push(
                Router::new()
                    .delete(delete_pipeline_queue_item)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs")
            .push(
                Router::new()
                    .get(get_pipeline_runs)
            )
            .push(
                Router::new()
                    .post(post_pipeline_runs)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}")
            .push(
                Router::new()
                    .get(get_pipeline_run)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/log
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/log")
            .push(
                Router::new()
                    .get(get_pipeline_run_log)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes")
            .push(
                Router::new()
                    .get(get_pipeline_run_nodes)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}")
            .push(
                Router::new()
                    .get(get_pipeline_run_node)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps")
            .push(
                Router::new()
                    .get(get_pipeline_run_node_steps)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}")
            .push(
                Router::new()
                    .get(get_pipeline_run_node_step)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}/log
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}/log")
            .push(
                Router::new()
                    .get(get_pipeline_run_node_step_log)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/replay
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/replay")
            .push(
                Router::new()
                    .post(post_pipeline_run)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/stop
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/stop")
            .push(
                Router::new()
                    .put(put_pipeline_run)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/scm/{scm}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/scm/{scm}")
            .push(
                Router::new()
                    .get(get_scm)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/scm/{scm}/organizations
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/scm/{scm}/organizations")
            .push(
                Router::new()
                    .get(get_scm_organisations)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories")
            .push(
                Router::new()
                    .get(get_scm_organisation_repositories)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories/{repository}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories/{repository}")
            .push(
                Router::new()
                    .get(get_scm_organisation_repository)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/user/
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/user/")
            .push(
                Router::new()
                    .get(get_authenticated_user)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/users/
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/users/")
            .push(
                Router::new()
                    .get(get_users)
            )
    );
    // Routes for /blue/rest/organizations/{organization}/users/{user}
    router = router.push(
        Router::with_path("/blue/rest/organizations/{organization}/users/{user}")
            .push(
                Router::new()
                    .get(get_user)
            )
    );
    // Routes for /blue/rest/search/
    router = router.push(
        Router::with_path("/blue/rest/search/")
            .push(
                Router::new()
                    .get(search)
            )
    );
    // Routes for /blue/rest/users/{user}/favorites
    router = router.push(
        Router::with_path("/blue/rest/users/{user}/favorites")
            .push(
                Router::new()
                    .get(get_user_favorites)
            )
    );
    // Routes for /computer/api/json
    router = router.push(
        Router::with_path("/computer/api/json")
            .push(
                Router::new()
                    .get(get_computer)
            )
    );
    // Routes for /createItem
    router = router.push(
        Router::with_path("/createItem")
            .push(
                Router::new()
                    .post(post_create_item)
            )
    );
    // Routes for /createView
    router = router.push(
        Router::with_path("/createView")
            .push(
                Router::new()
                    .post(post_create_view)
            )
    );
    // Routes for /crumbIssuer/api/json
    router = router.push(
        Router::with_path("/crumbIssuer/api/json")
            .push(
                Router::new()
                    .get(get_crumb)
            )
    );
    // Routes for /job/{name}/api/json
    router = router.push(
        Router::with_path("/job/{name}/api/json")
            .push(
                Router::new()
                    .get(get_job)
            )
    );
    // Routes for /job/{name}/build
    router = router.push(
        Router::with_path("/job/{name}/build")
            .push(
                Router::new()
                    .post(post_job_build)
            )
    );
    // Routes for /job/{name}/config.xml
    router = router.push(
        Router::with_path("/job/{name}/config.xml")
            .push(
                Router::new()
                    .get(get_job_config)
            )
            .push(
                Router::new()
                    .post(post_job_config)
            )
    );
    // Routes for /job/{name}/disable
    router = router.push(
        Router::with_path("/job/{name}/disable")
            .push(
                Router::new()
                    .post(post_job_disable)
            )
    );
    // Routes for /job/{name}/doDelete
    router = router.push(
        Router::with_path("/job/{name}/doDelete")
            .push(
                Router::new()
                    .post(post_job_delete)
            )
    );
    // Routes for /job/{name}/enable
    router = router.push(
        Router::with_path("/job/{name}/enable")
            .push(
                Router::new()
                    .post(post_job_enable)
            )
    );
    // Routes for /job/{name}/lastBuild/api/json
    router = router.push(
        Router::with_path("/job/{name}/lastBuild/api/json")
            .push(
                Router::new()
                    .get(get_job_last_build)
            )
    );
    // Routes for /job/{name}/lastBuild/stop
    router = router.push(
        Router::with_path("/job/{name}/lastBuild/stop")
            .push(
                Router::new()
                    .post(post_job_last_build_stop)
            )
    );
    // Routes for /job/{name}/{number}/logText/progressiveText
    router = router.push(
        Router::with_path("/job/{name}/{number}/logText/progressiveText")
            .push(
                Router::new()
                    .get(get_job_progressive_text)
            )
    );
    // Routes for /jwt-auth/jwks/{key}
    router = router.push(
        Router::with_path("/jwt-auth/jwks/{key}")
            .push(
                Router::new()
                    .get(get_json_web_key)
            )
    );
    // Routes for /jwt-auth/token
    router = router.push(
        Router::with_path("/jwt-auth/token")
            .push(
                Router::new()
                    .get(get_json_web_token)
            )
    );
    // Routes for /queue/api/json
    router = router.push(
        Router::with_path("/queue/api/json")
            .push(
                Router::new()
                    .get(get_queue)
            )
    );
    // Routes for /queue/item/{number}/api/json
    router = router.push(
        Router::with_path("/queue/item/{number}/api/json")
            .push(
                Router::new()
                    .get(get_queue_item)
            )
    );
    // Routes for /view/{name}/api/json
    router = router.push(
        Router::with_path("/view/{name}/api/json")
            .push(
                Router::new()
                    .get(get_view)
            )
    );
    // Routes for /view/{name}/config.xml
    router = router.push(
        Router::with_path("/view/{name}/config.xml")
            .push(
                Router::new()
                    .get(get_view_config)
            )
            .push(
                Router::new()
                    .post(post_view_config)
            )
    );

    router
}
