/**
 * Swaggy Jenkins
 * Jenkins API clients generated from Swagger / Open API specification
 *
 * OpenAPI spec version: 3.3.1-pre.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PipelineImpllinksData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PipelineImpllinksData(
  self: LinkData = null ,
  actions: LinkData = null ,
  runs: LinkData = null ,
  queue: LinkData = null ,
  `class`: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== self validation ==================
        
        
        
        
        // validating self
        if (_allValidationErrors.isEmpty || !failFast) {
            if self != null then _allValidationErrors ++= self.validationErrors(path :+ PipelineImpllinks.Fields.self, failFast)
        }

        // ================== actions validation ==================
        
        
        
        
        // validating actions
        if (_allValidationErrors.isEmpty || !failFast) {
            if actions != null then _allValidationErrors ++= actions.validationErrors(path :+ PipelineImpllinks.Fields.actions, failFast)
        }

        // ================== runs validation ==================
        
        
        
        
        // validating runs
        if (_allValidationErrors.isEmpty || !failFast) {
            if runs != null then _allValidationErrors ++= runs.validationErrors(path :+ PipelineImpllinks.Fields.runs, failFast)
        }

        // ================== queue validation ==================
        
        
        
        
        // validating queue
        if (_allValidationErrors.isEmpty || !failFast) {
            if queue != null then _allValidationErrors ++= queue.validationErrors(path :+ PipelineImpllinks.Fields.queue, failFast)
        }

        // ================== `class` validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PipelineImpllinks] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PipelineImpllinks = {
    PipelineImpllinks(
        self = Option(self).map(_.asModel) /* 4 */,
        actions = Option(actions).map(_.asModel) /* 4 */,
        runs = Option(runs).map(_.asModel) /* 4 */,
        queue = Option(queue).map(_.asModel) /* 4 */,
        `class` = Option(`class`) /* 1 */
    
    )
  }
}

object PipelineImpllinksData {

  def validated(d8a : PipelineImpllinksData, failFast : Boolean) : scala.util.Try[PipelineImpllinks] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PipelineImpllinksData = try {
        val data = read[PipelineImpllinksData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PipelineImpllinksData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PipelineImpllinksData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PipelineImpllinksData] = try {
        read[List[PipelineImpllinksData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PipelineImpllinks]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PipelineImpllinks]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PipelineImpllinksData] = try {
        read[Map[String, PipelineImpllinksData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PipelineImpllinks]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PipelineImpllinks]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

