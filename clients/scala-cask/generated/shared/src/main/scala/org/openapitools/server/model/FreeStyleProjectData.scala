/**
 * Swaggy Jenkins
 * Jenkins API clients generated from Swagger / Open API specification
 *
 * OpenAPI spec version: 3.3.1-pre.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** FreeStyleProjectData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class FreeStyleProjectData(
  `class`: String = "" ,
  name: String = "" ,
  url: String = "" ,
  color: String = "" ,
  actions: Seq[FreeStyleProjectactionsData] = Nil ,
  description: String = "" ,
  displayName: String = "" ,
  displayNameOrNull: String = "" ,
  fullDisplayName: String = "" ,
  fullName: String = "" ,
  buildable: Boolean = false ,
  builds: Seq[FreeStyleBuildData] = Nil ,
  firstBuild: FreeStyleBuildData = null ,
  healthReport: Seq[FreeStyleProjecthealthReportData] = Nil ,
  inQueue: Boolean = false ,
  keepDependencies: Boolean = false ,
  lastBuild: FreeStyleBuildData = null ,
  lastCompletedBuild: FreeStyleBuildData = null ,
  lastFailedBuild: String = "" ,
  lastStableBuild: FreeStyleBuildData = null ,
  lastSuccessfulBuild: FreeStyleBuildData = null ,
  lastUnstableBuild: String = "" ,
  lastUnsuccessfulBuild: String = "" ,
  nextBuildNumber: Int = 0 ,
  queueItem: String = "" ,
  concurrentBuild: Boolean = false ,
  scm: NullSCMData = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== `class` validation ==================
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== url validation ==================
        
        
        
        

        // ================== color validation ==================
        
        
        
        

        // ================== actions validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (actions != null) {
                actions.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ FreeStyleProject.Fields.actions :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== description validation ==================
        
        
        
        

        // ================== displayName validation ==================
        
        
        
        

        // ================== displayNameOrNull validation ==================
        
        
        
        

        // ================== fullDisplayName validation ==================
        
        
        
        

        // ================== fullName validation ==================
        
        
        
        

        // ================== buildable validation ==================
        
        
        
        

        // ================== builds validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (builds != null) {
                builds.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ FreeStyleProject.Fields.builds :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== firstBuild validation ==================
        
        
        
        
        // validating firstBuild
        if (_allValidationErrors.isEmpty || !failFast) {
            if firstBuild != null then _allValidationErrors ++= firstBuild.validationErrors(path :+ FreeStyleProject.Fields.firstBuild, failFast)
        }

        // ================== healthReport validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (healthReport != null) {
                healthReport.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ FreeStyleProject.Fields.healthReport :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== inQueue validation ==================
        
        
        
        

        // ================== keepDependencies validation ==================
        
        
        
        

        // ================== lastBuild validation ==================
        
        
        
        
        // validating lastBuild
        if (_allValidationErrors.isEmpty || !failFast) {
            if lastBuild != null then _allValidationErrors ++= lastBuild.validationErrors(path :+ FreeStyleProject.Fields.lastBuild, failFast)
        }

        // ================== lastCompletedBuild validation ==================
        
        
        
        
        // validating lastCompletedBuild
        if (_allValidationErrors.isEmpty || !failFast) {
            if lastCompletedBuild != null then _allValidationErrors ++= lastCompletedBuild.validationErrors(path :+ FreeStyleProject.Fields.lastCompletedBuild, failFast)
        }

        // ================== lastFailedBuild validation ==================
        
        
        
        

        // ================== lastStableBuild validation ==================
        
        
        
        
        // validating lastStableBuild
        if (_allValidationErrors.isEmpty || !failFast) {
            if lastStableBuild != null then _allValidationErrors ++= lastStableBuild.validationErrors(path :+ FreeStyleProject.Fields.lastStableBuild, failFast)
        }

        // ================== lastSuccessfulBuild validation ==================
        
        
        
        
        // validating lastSuccessfulBuild
        if (_allValidationErrors.isEmpty || !failFast) {
            if lastSuccessfulBuild != null then _allValidationErrors ++= lastSuccessfulBuild.validationErrors(path :+ FreeStyleProject.Fields.lastSuccessfulBuild, failFast)
        }

        // ================== lastUnstableBuild validation ==================
        
        
        
        

        // ================== lastUnsuccessfulBuild validation ==================
        
        
        
        

        // ================== nextBuildNumber validation ==================
        
        
        
        

        // ================== queueItem validation ==================
        
        
        
        

        // ================== concurrentBuild validation ==================
        
        
        
        

        // ================== scm validation ==================
        
        
        
        
        // validating scm
        if (_allValidationErrors.isEmpty || !failFast) {
            if scm != null then _allValidationErrors ++= scm.validationErrors(path :+ FreeStyleProject.Fields.scm, failFast)
        }

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[FreeStyleProject] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : FreeStyleProject = {
    FreeStyleProject(
        `class` = Option(`class`) /* 1 */,
        name = Option(name) /* 1 */,
        url = Option(url) /* 1 */,
        color = Option(color) /* 1 */,
        actions = actions.map(_.asModel) /* 5 */,
        description = Option(description) /* 1 */,
        displayName = Option(displayName) /* 1 */,
        displayNameOrNull = Option(displayNameOrNull) /* 1 */,
        fullDisplayName = Option(fullDisplayName) /* 1 */,
        fullName = Option(fullName) /* 1 */,
        buildable = Option(buildable) /* 1 */,
        builds = builds.map(_.asModel) /* 5 */,
        firstBuild = Option(firstBuild).map(_.asModel) /* 4 */,
        healthReport = healthReport.map(_.asModel) /* 5 */,
        inQueue = Option(inQueue) /* 1 */,
        keepDependencies = Option(keepDependencies) /* 1 */,
        lastBuild = Option(lastBuild).map(_.asModel) /* 4 */,
        lastCompletedBuild = Option(lastCompletedBuild).map(_.asModel) /* 4 */,
        lastFailedBuild = Option(lastFailedBuild) /* 1 */,
        lastStableBuild = Option(lastStableBuild).map(_.asModel) /* 4 */,
        lastSuccessfulBuild = Option(lastSuccessfulBuild).map(_.asModel) /* 4 */,
        lastUnstableBuild = Option(lastUnstableBuild) /* 1 */,
        lastUnsuccessfulBuild = Option(lastUnsuccessfulBuild) /* 1 */,
        nextBuildNumber = Option(nextBuildNumber) /* 1 */,
        queueItem = Option(queueItem) /* 1 */,
        concurrentBuild = Option(concurrentBuild) /* 1 */,
        scm = Option(scm).map(_.asModel) /* 4 */
    
    )
  }
}

object FreeStyleProjectData {

  def validated(d8a : FreeStyleProjectData, failFast : Boolean) : scala.util.Try[FreeStyleProject] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : FreeStyleProjectData = try {
        val data = read[FreeStyleProjectData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating FreeStyleProjectData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : FreeStyleProjectData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[FreeStyleProjectData] = try {
        read[List[FreeStyleProjectData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[FreeStyleProject]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[FreeStyleProject]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, FreeStyleProjectData] = try {
        read[Map[String, FreeStyleProjectData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, FreeStyleProject]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, FreeStyleProject]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

