/**
 * Swaggy Jenkins
 * Jenkins API clients generated from Swagger / Open API specification
 *
 * OpenAPI spec version: 3.4.1-pre.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** HudsonData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class HudsonData(
  `class`: String = "" ,
  assignedLabels: Seq[HudsonassignedLabelsData] = Nil ,
  mode: String = "" ,
  nodeDescription: String = "" ,
  nodeName: String = "" ,
  numExecutors: Int = 0 ,
  description: String = "" ,
  jobs: Seq[FreeStyleProjectData] = Nil ,
  primaryView: AllViewData = null ,
  quietingDown: Boolean = false ,
  slaveAgentPort: Int = 0 ,
  unlabeledLoad: UnlabeledLoadStatisticsData = null ,
  useCrumbs: Boolean = false ,
  useSecurity: Boolean = false ,
  views: Seq[AllViewData] = Nil 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== `class` validation ==================
        
        
        
        

        // ================== assignedLabels validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (assignedLabels != null) {
                assignedLabels.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ Hudson.Fields.assignedLabels :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== mode validation ==================
        
        
        
        

        // ================== nodeDescription validation ==================
        
        
        
        

        // ================== nodeName validation ==================
        
        
        
        

        // ================== numExecutors validation ==================
        
        
        
        

        // ================== description validation ==================
        
        
        
        

        // ================== jobs validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (jobs != null) {
                jobs.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ Hudson.Fields.jobs :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== primaryView validation ==================
        
        
        
        
        // validating primaryView
        if (_allValidationErrors.isEmpty || !failFast) {
            if primaryView != null then _allValidationErrors ++= primaryView.validationErrors(path :+ Hudson.Fields.primaryView, failFast)
        }

        // ================== quietingDown validation ==================
        
        
        
        

        // ================== slaveAgentPort validation ==================
        
        
        
        

        // ================== unlabeledLoad validation ==================
        
        
        
        
        // validating unlabeledLoad
        if (_allValidationErrors.isEmpty || !failFast) {
            if unlabeledLoad != null then _allValidationErrors ++= unlabeledLoad.validationErrors(path :+ Hudson.Fields.unlabeledLoad, failFast)
        }

        // ================== useCrumbs validation ==================
        
        
        
        

        // ================== useSecurity validation ==================
        
        
        
        

        // ================== views validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (views != null) {
                views.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ Hudson.Fields.views :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[Hudson] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : Hudson = {
    Hudson(
        `class` = Option(`class`) /* 1 */,
        assignedLabels = assignedLabels.map(_.asModel) /* 5 */,
        mode = Option(mode) /* 1 */,
        nodeDescription = Option(nodeDescription) /* 1 */,
        nodeName = Option(nodeName) /* 1 */,
        numExecutors = Option(numExecutors) /* 1 */,
        description = Option(description) /* 1 */,
        jobs = jobs.map(_.asModel) /* 5 */,
        primaryView = Option(primaryView).map(_.asModel) /* 4 */,
        quietingDown = Option(quietingDown) /* 1 */,
        slaveAgentPort = Option(slaveAgentPort) /* 1 */,
        unlabeledLoad = Option(unlabeledLoad).map(_.asModel) /* 4 */,
        useCrumbs = Option(useCrumbs) /* 1 */,
        useSecurity = Option(useSecurity) /* 1 */,
        views = views.map(_.asModel) /* 5 */
    
    )
  }
}

object HudsonData {

  def validated(d8a : HudsonData, failFast : Boolean) : scala.util.Try[Hudson] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : HudsonData = try {
        val data = read[HudsonData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating HudsonData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : HudsonData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[HudsonData] = try {
        read[List[HudsonData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[Hudson]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[Hudson]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, HudsonData] = try {
        read[Map[String, HudsonData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, Hudson]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, Hudson]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

