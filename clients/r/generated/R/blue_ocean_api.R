#' Swaggy Jenkins
#'
#' Jenkins API clients generated from Swagger / Open API specification
#'
#' The version of the OpenAPI document: 3.2.1-pre.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title BlueOcean operations
#' @description BlueOceanApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  DeletePipelineQueueItem  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_queue <- "queue_example" # character | Name of the queue item
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' api_instance$DeletePipelineQueueItem(var_organization, var_pipeline, var_queue)
#'
#'
#' ####################  GetAuthenticatedUser  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthenticatedUser(var_organizationdata_file = "result.txt")
#' result <- api_instance$GetAuthenticatedUser(var_organization)
#' dput(result)
#'
#'
#' ####################  GetClasses  ####################
#'
#' library(openapi)
#' var_class <- "class_example" # character | Name of the class
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetClasses(var_classdata_file = "result.txt")
#' result <- api_instance$GetClasses(var_class)
#' dput(result)
#'
#'
#' ####################  GetJsonWebKey  ####################
#'
#' library(openapi)
#' var_key <- 56 # integer | Key ID received as part of JWT header field kid
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetJsonWebKey(var_keydata_file = "result.txt")
#' result <- api_instance$GetJsonWebKey(var_key)
#' dput(result)
#'
#'
#' ####################  GetJsonWebToken  ####################
#'
#' library(openapi)
#' var_expiry_time_in_mins <- 56 # integer | Token expiry time in minutes, default: 30 minutes (Optional)
#' var_max_expiry_time_in_mins <- 56 # integer | Maximum token expiry time in minutes, default: 480 minutes (Optional)
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetJsonWebToken(expiry_time_in_mins = var_expiry_time_in_mins, max_expiry_time_in_mins = var_max_expiry_time_in_minsdata_file = "result.txt")
#' result <- api_instance$GetJsonWebToken(expiry_time_in_mins = var_expiry_time_in_mins, max_expiry_time_in_mins = var_max_expiry_time_in_mins)
#' dput(result)
#'
#'
#' ####################  GetOrganisation  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOrganisation(var_organizationdata_file = "result.txt")
#' result <- api_instance$GetOrganisation(var_organization)
#' dput(result)
#'
#'
#' ####################  GetOrganisations  ####################
#'
#' library(openapi)
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOrganisations(data_file = "result.txt")
#' result <- api_instance$GetOrganisations()
#' dput(result)
#'
#'
#' ####################  GetPipeline  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipeline(var_organization, var_pipelinedata_file = "result.txt")
#' result <- api_instance$GetPipeline(var_organization, var_pipeline)
#' dput(result)
#'
#'
#' ####################  GetPipelineActivities  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineActivities(var_organization, var_pipelinedata_file = "result.txt")
#' result <- api_instance$GetPipelineActivities(var_organization, var_pipeline)
#' dput(result)
#'
#'
#' ####################  GetPipelineBranch  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_branch <- "branch_example" # character | Name of the branch
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineBranch(var_organization, var_pipeline, var_branchdata_file = "result.txt")
#' result <- api_instance$GetPipelineBranch(var_organization, var_pipeline, var_branch)
#' dput(result)
#'
#'
#' ####################  GetPipelineBranchRun  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_branch <- "branch_example" # character | Name of the branch
#' var_run <- "run_example" # character | Name of the run
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineBranchRun(var_organization, var_pipeline, var_branch, var_rundata_file = "result.txt")
#' result <- api_instance$GetPipelineBranchRun(var_organization, var_pipeline, var_branch, var_run)
#' dput(result)
#'
#'
#' ####################  GetPipelineBranches  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineBranches(var_organization, var_pipelinedata_file = "result.txt")
#' result <- api_instance$GetPipelineBranches(var_organization, var_pipeline)
#' dput(result)
#'
#'
#' ####################  GetPipelineFolder  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_folder <- "folder_example" # character | Name of the folder
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineFolder(var_organization, var_folderdata_file = "result.txt")
#' result <- api_instance$GetPipelineFolder(var_organization, var_folder)
#' dput(result)
#'
#'
#' ####################  GetPipelineFolderPipeline  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_folder <- "folder_example" # character | Name of the folder
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineFolderPipeline(var_organization, var_pipeline, var_folderdata_file = "result.txt")
#' result <- api_instance$GetPipelineFolderPipeline(var_organization, var_pipeline, var_folder)
#' dput(result)
#'
#'
#' ####################  GetPipelineQueue  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineQueue(var_organization, var_pipelinedata_file = "result.txt")
#' result <- api_instance$GetPipelineQueue(var_organization, var_pipeline)
#' dput(result)
#'
#'
#' ####################  GetPipelineRun  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRun(var_organization, var_pipeline, var_rundata_file = "result.txt")
#' result <- api_instance$GetPipelineRun(var_organization, var_pipeline, var_run)
#' dput(result)
#'
#'
#' ####################  GetPipelineRunLog  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#' var_start <- 56 # integer | Start position of the log (Optional)
#' var_download <- "download_example" # character | Set to true in order to download the file, otherwise it's passed as a response body (Optional)
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRunLog(var_organization, var_pipeline, var_run, start = var_start, download = var_downloaddata_file = "result.txt")
#' result <- api_instance$GetPipelineRunLog(var_organization, var_pipeline, var_run, start = var_start, download = var_download)
#' dput(result)
#'
#'
#' ####################  GetPipelineRunNode  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#' var_node <- "node_example" # character | Name of the node
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRunNode(var_organization, var_pipeline, var_run, var_nodedata_file = "result.txt")
#' result <- api_instance$GetPipelineRunNode(var_organization, var_pipeline, var_run, var_node)
#' dput(result)
#'
#'
#' ####################  GetPipelineRunNodeStep  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#' var_node <- "node_example" # character | Name of the node
#' var_step <- "step_example" # character | Name of the step
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRunNodeStep(var_organization, var_pipeline, var_run, var_node, var_stepdata_file = "result.txt")
#' result <- api_instance$GetPipelineRunNodeStep(var_organization, var_pipeline, var_run, var_node, var_step)
#' dput(result)
#'
#'
#' ####################  GetPipelineRunNodeStepLog  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#' var_node <- "node_example" # character | Name of the node
#' var_step <- "step_example" # character | Name of the step
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRunNodeStepLog(var_organization, var_pipeline, var_run, var_node, var_stepdata_file = "result.txt")
#' result <- api_instance$GetPipelineRunNodeStepLog(var_organization, var_pipeline, var_run, var_node, var_step)
#' dput(result)
#'
#'
#' ####################  GetPipelineRunNodeSteps  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#' var_node <- "node_example" # character | Name of the node
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRunNodeSteps(var_organization, var_pipeline, var_run, var_nodedata_file = "result.txt")
#' result <- api_instance$GetPipelineRunNodeSteps(var_organization, var_pipeline, var_run, var_node)
#' dput(result)
#'
#'
#' ####################  GetPipelineRunNodes  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRunNodes(var_organization, var_pipeline, var_rundata_file = "result.txt")
#' result <- api_instance$GetPipelineRunNodes(var_organization, var_pipeline, var_run)
#' dput(result)
#'
#'
#' ####################  GetPipelineRuns  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelineRuns(var_organization, var_pipelinedata_file = "result.txt")
#' result <- api_instance$GetPipelineRuns(var_organization, var_pipeline)
#' dput(result)
#'
#'
#' ####################  GetPipelines  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPipelines(var_organizationdata_file = "result.txt")
#' result <- api_instance$GetPipelines(var_organization)
#' dput(result)
#'
#'
#' ####################  GetSCM  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_scm <- "scm_example" # character | Name of SCM
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSCM(var_organization, var_scmdata_file = "result.txt")
#' result <- api_instance$GetSCM(var_organization, var_scm)
#' dput(result)
#'
#'
#' ####################  GetSCMOrganisationRepositories  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_scm <- "scm_example" # character | Name of SCM
#' var_scm_organisation <- "scm_organisation_example" # character | Name of the SCM organization
#' var_credential_id <- "credential_id_example" # character | Credential ID (Optional)
#' var_page_size <- 56 # integer | Number of items in a page (Optional)
#' var_page_number <- 56 # integer | Page number (Optional)
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSCMOrganisationRepositories(var_organization, var_scm, var_scm_organisation, credential_id = var_credential_id, page_size = var_page_size, page_number = var_page_numberdata_file = "result.txt")
#' result <- api_instance$GetSCMOrganisationRepositories(var_organization, var_scm, var_scm_organisation, credential_id = var_credential_id, page_size = var_page_size, page_number = var_page_number)
#' dput(result)
#'
#'
#' ####################  GetSCMOrganisationRepository  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_scm <- "scm_example" # character | Name of SCM
#' var_scm_organisation <- "scm_organisation_example" # character | Name of the SCM organization
#' var_repository <- "repository_example" # character | Name of the SCM repository
#' var_credential_id <- "credential_id_example" # character | Credential ID (Optional)
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSCMOrganisationRepository(var_organization, var_scm, var_scm_organisation, var_repository, credential_id = var_credential_iddata_file = "result.txt")
#' result <- api_instance$GetSCMOrganisationRepository(var_organization, var_scm, var_scm_organisation, var_repository, credential_id = var_credential_id)
#' dput(result)
#'
#'
#' ####################  GetSCMOrganisations  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_scm <- "scm_example" # character | Name of SCM
#' var_credential_id <- "credential_id_example" # character | Credential ID (Optional)
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSCMOrganisations(var_organization, var_scm, credential_id = var_credential_iddata_file = "result.txt")
#' result <- api_instance$GetSCMOrganisations(var_organization, var_scm, credential_id = var_credential_id)
#' dput(result)
#'
#'
#' ####################  GetUser  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_user <- "user_example" # character | Name of the user
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetUser(var_organization, var_userdata_file = "result.txt")
#' result <- api_instance$GetUser(var_organization, var_user)
#' dput(result)
#'
#'
#' ####################  GetUserFavorites  ####################
#'
#' library(openapi)
#' var_user <- "user_example" # character | Name of the user
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetUserFavorites(var_userdata_file = "result.txt")
#' result <- api_instance$GetUserFavorites(var_user)
#' dput(result)
#'
#'
#' ####################  GetUsers  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetUsers(var_organizationdata_file = "result.txt")
#' result <- api_instance$GetUsers(var_organization)
#' dput(result)
#'
#'
#' ####################  PostPipelineRun  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostPipelineRun(var_organization, var_pipeline, var_rundata_file = "result.txt")
#' result <- api_instance$PostPipelineRun(var_organization, var_pipeline, var_run)
#' dput(result)
#'
#'
#' ####################  PostPipelineRuns  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostPipelineRuns(var_organization, var_pipelinedata_file = "result.txt")
#' result <- api_instance$PostPipelineRuns(var_organization, var_pipeline)
#' dput(result)
#'
#'
#' ####################  PutPipelineFavorite  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_body <- "body_example" # character | Set JSON string body to {\"favorite\": true} to favorite, set value to false to unfavorite
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutPipelineFavorite(var_organization, var_pipeline, var_bodydata_file = "result.txt")
#' result <- api_instance$PutPipelineFavorite(var_organization, var_pipeline, var_body)
#' dput(result)
#'
#'
#' ####################  PutPipelineRun  ####################
#'
#' library(openapi)
#' var_organization <- "organization_example" # character | Name of the organization
#' var_pipeline <- "pipeline_example" # character | Name of the pipeline
#' var_run <- "run_example" # character | Name of the run
#' var_blocking <- "blocking_example" # character | Set to true to make blocking stop, default: false (Optional)
#' var_time_out_in_secs <- 56 # integer | Timeout in seconds, default: 10 seconds (Optional)
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutPipelineRun(var_organization, var_pipeline, var_run, blocking = var_blocking, time_out_in_secs = var_time_out_in_secsdata_file = "result.txt")
#' result <- api_instance$PutPipelineRun(var_organization, var_pipeline, var_run, blocking = var_blocking, time_out_in_secs = var_time_out_in_secs)
#' dput(result)
#'
#'
#' ####################  Search  ####################
#'
#' library(openapi)
#' var_q <- "q_example" # character | Query string
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$Search(var_qdata_file = "result.txt")
#' result <- api_instance$Search(var_q)
#' dput(result)
#'
#'
#' ####################  SearchClasses  ####################
#'
#' library(openapi)
#' var_q <- "q_example" # character | Query string containing an array of class names
#'
#' api_instance <- BlueOceanApi$new()
#'
#' # Configure HTTP basic authorization: jenkins_auth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchClasses(var_qdata_file = "result.txt")
#' result <- api_instance$SearchClasses(var_q)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BlueOceanApi <- R6::R6Class(
  "BlueOceanApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BlueOceanApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param queue Name of the queue item
    #' @param ... Other optional arguments
    #'
    #' @return void
    DeletePipelineQueueItem = function(organization, pipeline, queue, ...) {
      local_var_response <- self$DeletePipelineQueueItemWithHttpInfo(organization, pipeline, queue, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param queue Name of the queue item
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    DeletePipelineQueueItemWithHttpInfo = function(organization, pipeline, queue, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`queue`)) {
        stop("Missing required parameter `queue`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$DeletePipelineQueueItem, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$DeletePipelineQueueItem, `pipeline` is not nullable")
      }

      if (!missing(`queue`) && is.null(`queue`)) {
        stop("Invalid value for `queue` when calling BlueOceanApi$DeletePipelineQueueItem, `queue` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/queue/{queue}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`queue`)) {
        local_var_url_path <- gsub("\\{queue\\}", URLencode(as.character(`queue`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return User
    GetAuthenticatedUser = function(organization, data_file = NULL, ...) {
      local_var_response <- self$GetAuthenticatedUserWithHttpInfo(organization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (User) with additional information such as HTTP status code, headers
    GetAuthenticatedUserWithHttpInfo = function(organization, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetAuthenticatedUser, `organization` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/user/"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "User"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param class Name of the class
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return character
    GetClasses = function(class, data_file = NULL, ...) {
      local_var_response <- self$GetClassesWithHttpInfo(class, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param class Name of the class
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    GetClassesWithHttpInfo = function(class, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`class`)) {
        stop("Missing required parameter `class`.")
      }

      if (!missing(`class`) && is.null(`class`)) {
        stop("Invalid value for `class` when calling BlueOceanApi$GetClasses, `class` is not nullable")
      }

      local_var_url_path <- "/blue/rest/classes/{class}"
      if (!missing(`class`)) {
        local_var_url_path <- gsub("\\{class\\}", URLencode(as.character(`class`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param key Key ID received as part of JWT header field kid
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return character
    GetJsonWebKey = function(key, data_file = NULL, ...) {
      local_var_response <- self$GetJsonWebKeyWithHttpInfo(key, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param key Key ID received as part of JWT header field kid
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    GetJsonWebKeyWithHttpInfo = function(key, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`key`)) {
        stop("Missing required parameter `key`.")
      }

      if (!missing(`key`) && is.null(`key`)) {
        stop("Invalid value for `key` when calling BlueOceanApi$GetJsonWebKey, `key` is not nullable")
      }

      local_var_url_path <- "/jwt-auth/jwks/{key}"
      if (!missing(`key`)) {
        local_var_url_path <- gsub("\\{key\\}", URLencode(as.character(`key`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param expiry_time_in_mins (optional) Token expiry time in minutes, default: 30 minutes
    #' @param max_expiry_time_in_mins (optional) Maximum token expiry time in minutes, default: 480 minutes
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return character
    GetJsonWebToken = function(expiry_time_in_mins = NULL, max_expiry_time_in_mins = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetJsonWebTokenWithHttpInfo(expiry_time_in_mins, max_expiry_time_in_mins, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param expiry_time_in_mins (optional) Token expiry time in minutes, default: 30 minutes
    #' @param max_expiry_time_in_mins (optional) Maximum token expiry time in minutes, default: 480 minutes
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    GetJsonWebTokenWithHttpInfo = function(expiry_time_in_mins = NULL, max_expiry_time_in_mins = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`expiry_time_in_mins`) && is.null(`expiry_time_in_mins`)) {
        stop("Invalid value for `expiry_time_in_mins` when calling BlueOceanApi$GetJsonWebToken, `expiry_time_in_mins` is not nullable")
      }

      if (!missing(`max_expiry_time_in_mins`) && is.null(`max_expiry_time_in_mins`)) {
        stop("Invalid value for `max_expiry_time_in_mins` when calling BlueOceanApi$GetJsonWebToken, `max_expiry_time_in_mins` is not nullable")
      }

      query_params[["expiryTimeInMins"]] <- `expiry_time_in_mins`

      query_params[["maxExpiryTimeInMins"]] <- `max_expiry_time_in_mins`

      local_var_url_path <- "/jwt-auth/token"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Organisation
    GetOrganisation = function(organization, data_file = NULL, ...) {
      local_var_response <- self$GetOrganisationWithHttpInfo(organization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Organisation) with additional information such as HTTP status code, headers
    GetOrganisationWithHttpInfo = function(organization, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetOrganisation, `organization` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Organisation"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[Organisation]
    GetOrganisations = function(data_file = NULL, ...) {
      local_var_response <- self$GetOrganisationsWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[Organisation]) with additional information such as HTTP status code, headers
    GetOrganisationsWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/blue/rest/organizations/"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[Organisation]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Pipeline
    GetPipeline = function(organization, pipeline, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineWithHttpInfo(organization, pipeline, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Pipeline) with additional information such as HTTP status code, headers
    GetPipelineWithHttpInfo = function(organization, pipeline, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipeline, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipeline, `pipeline` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Pipeline"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[PipelineActivity]
    GetPipelineActivities = function(organization, pipeline, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineActivitiesWithHttpInfo(organization, pipeline, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[PipelineActivity]) with additional information such as HTTP status code, headers
    GetPipelineActivitiesWithHttpInfo = function(organization, pipeline, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineActivities, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineActivities, `pipeline` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/activities"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[PipelineActivity]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param branch Name of the branch
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BranchImpl
    GetPipelineBranch = function(organization, pipeline, branch, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineBranchWithHttpInfo(organization, pipeline, branch, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param branch Name of the branch
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BranchImpl) with additional information such as HTTP status code, headers
    GetPipelineBranchWithHttpInfo = function(organization, pipeline, branch, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`branch`)) {
        stop("Missing required parameter `branch`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineBranch, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineBranch, `pipeline` is not nullable")
      }

      if (!missing(`branch`) && is.null(`branch`)) {
        stop("Invalid value for `branch` when calling BlueOceanApi$GetPipelineBranch, `branch` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}/"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`branch`)) {
        local_var_url_path <- gsub("\\{branch\\}", URLencode(as.character(`branch`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BranchImpl"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param branch Name of the branch
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PipelineRun
    GetPipelineBranchRun = function(organization, pipeline, branch, run, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineBranchRunWithHttpInfo(organization, pipeline, branch, run, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param branch Name of the branch
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PipelineRun) with additional information such as HTTP status code, headers
    GetPipelineBranchRunWithHttpInfo = function(organization, pipeline, branch, run, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`branch`)) {
        stop("Missing required parameter `branch`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineBranchRun, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineBranchRun, `pipeline` is not nullable")
      }

      if (!missing(`branch`) && is.null(`branch`)) {
        stop("Invalid value for `branch` when calling BlueOceanApi$GetPipelineBranchRun, `branch` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineBranchRun, `run` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}/runs/{run}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`branch`)) {
        local_var_url_path <- gsub("\\{branch\\}", URLencode(as.character(`branch`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PipelineRun"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return MultibranchPipeline
    GetPipelineBranches = function(organization, pipeline, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineBranchesWithHttpInfo(organization, pipeline, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (MultibranchPipeline) with additional information such as HTTP status code, headers
    GetPipelineBranchesWithHttpInfo = function(organization, pipeline, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineBranches, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineBranches, `pipeline` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MultibranchPipeline"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param folder Name of the folder
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PipelineFolderImpl
    GetPipelineFolder = function(organization, folder, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineFolderWithHttpInfo(organization, folder, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param folder Name of the folder
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PipelineFolderImpl) with additional information such as HTTP status code, headers
    GetPipelineFolderWithHttpInfo = function(organization, folder, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`folder`)) {
        stop("Missing required parameter `folder`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineFolder, `organization` is not nullable")
      }

      if (!missing(`folder`) && is.null(`folder`)) {
        stop("Invalid value for `folder` when calling BlueOceanApi$GetPipelineFolder, `folder` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{folder}/"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`folder`)) {
        local_var_url_path <- gsub("\\{folder\\}", URLencode(as.character(`folder`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PipelineFolderImpl"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param folder Name of the folder
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PipelineImpl
    GetPipelineFolderPipeline = function(organization, pipeline, folder, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineFolderPipelineWithHttpInfo(organization, pipeline, folder, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param folder Name of the folder
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PipelineImpl) with additional information such as HTTP status code, headers
    GetPipelineFolderPipelineWithHttpInfo = function(organization, pipeline, folder, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`folder`)) {
        stop("Missing required parameter `folder`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineFolderPipeline, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineFolderPipeline, `pipeline` is not nullable")
      }

      if (!missing(`folder`) && is.null(`folder`)) {
        stop("Invalid value for `folder` when calling BlueOceanApi$GetPipelineFolderPipeline, `folder` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{folder}/pipelines/{pipeline}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`folder`)) {
        local_var_url_path <- gsub("\\{folder\\}", URLencode(as.character(`folder`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PipelineImpl"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[QueueItemImpl]
    GetPipelineQueue = function(organization, pipeline, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineQueueWithHttpInfo(organization, pipeline, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[QueueItemImpl]) with additional information such as HTTP status code, headers
    GetPipelineQueueWithHttpInfo = function(organization, pipeline, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineQueue, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineQueue, `pipeline` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/queue"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[QueueItemImpl]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PipelineRun
    GetPipelineRun = function(organization, pipeline, run, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunWithHttpInfo(organization, pipeline, run, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PipelineRun) with additional information such as HTTP status code, headers
    GetPipelineRunWithHttpInfo = function(organization, pipeline, run, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRun, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRun, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineRun, `run` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PipelineRun"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param start (optional) Start position of the log
    #' @param download (optional) Set to true in order to download the file, otherwise it's passed as a response body
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return character
    GetPipelineRunLog = function(organization, pipeline, run, start = NULL, download = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunLogWithHttpInfo(organization, pipeline, run, start, download, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param start (optional) Start position of the log
    #' @param download (optional) Set to true in order to download the file, otherwise it's passed as a response body
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    GetPipelineRunLogWithHttpInfo = function(organization, pipeline, run, start = NULL, download = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRunLog, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRunLog, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineRunLog, `run` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling BlueOceanApi$GetPipelineRunLog, `start` is not nullable")
      }

      if (!missing(`download`) && is.null(`download`)) {
        stop("Invalid value for `download` when calling BlueOceanApi$GetPipelineRunLog, `download` is not nullable")
      }

      query_params[["start"]] <- `start`

      query_params[["download"]] <- `download`

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/log"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PipelineRunNode
    GetPipelineRunNode = function(organization, pipeline, run, node, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunNodeWithHttpInfo(organization, pipeline, run, node, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PipelineRunNode) with additional information such as HTTP status code, headers
    GetPipelineRunNodeWithHttpInfo = function(organization, pipeline, run, node, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (missing(`node`)) {
        stop("Missing required parameter `node`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRunNode, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRunNode, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineRunNode, `run` is not nullable")
      }

      if (!missing(`node`) && is.null(`node`)) {
        stop("Invalid value for `node` when calling BlueOceanApi$GetPipelineRunNode, `node` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`node`)) {
        local_var_url_path <- gsub("\\{node\\}", URLencode(as.character(`node`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PipelineRunNode"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param step Name of the step
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PipelineStepImpl
    GetPipelineRunNodeStep = function(organization, pipeline, run, node, step, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunNodeStepWithHttpInfo(organization, pipeline, run, node, step, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param step Name of the step
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PipelineStepImpl) with additional information such as HTTP status code, headers
    GetPipelineRunNodeStepWithHttpInfo = function(organization, pipeline, run, node, step, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (missing(`node`)) {
        stop("Missing required parameter `node`.")
      }

      if (missing(`step`)) {
        stop("Missing required parameter `step`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRunNodeStep, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRunNodeStep, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineRunNodeStep, `run` is not nullable")
      }

      if (!missing(`node`) && is.null(`node`)) {
        stop("Invalid value for `node` when calling BlueOceanApi$GetPipelineRunNodeStep, `node` is not nullable")
      }

      if (!missing(`step`) && is.null(`step`)) {
        stop("Invalid value for `step` when calling BlueOceanApi$GetPipelineRunNodeStep, `step` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`node`)) {
        local_var_url_path <- gsub("\\{node\\}", URLencode(as.character(`node`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`step`)) {
        local_var_url_path <- gsub("\\{step\\}", URLencode(as.character(`step`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PipelineStepImpl"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param step Name of the step
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return character
    GetPipelineRunNodeStepLog = function(organization, pipeline, run, node, step, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunNodeStepLogWithHttpInfo(organization, pipeline, run, node, step, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param step Name of the step
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    GetPipelineRunNodeStepLogWithHttpInfo = function(organization, pipeline, run, node, step, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (missing(`node`)) {
        stop("Missing required parameter `node`.")
      }

      if (missing(`step`)) {
        stop("Missing required parameter `step`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRunNodeStepLog, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRunNodeStepLog, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineRunNodeStepLog, `run` is not nullable")
      }

      if (!missing(`node`) && is.null(`node`)) {
        stop("Invalid value for `node` when calling BlueOceanApi$GetPipelineRunNodeStepLog, `node` is not nullable")
      }

      if (!missing(`step`) && is.null(`step`)) {
        stop("Invalid value for `step` when calling BlueOceanApi$GetPipelineRunNodeStepLog, `step` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}/log"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`node`)) {
        local_var_url_path <- gsub("\\{node\\}", URLencode(as.character(`node`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`step`)) {
        local_var_url_path <- gsub("\\{step\\}", URLencode(as.character(`step`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[PipelineStepImpl]
    GetPipelineRunNodeSteps = function(organization, pipeline, run, node, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunNodeStepsWithHttpInfo(organization, pipeline, run, node, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param node Name of the node
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[PipelineStepImpl]) with additional information such as HTTP status code, headers
    GetPipelineRunNodeStepsWithHttpInfo = function(organization, pipeline, run, node, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (missing(`node`)) {
        stop("Missing required parameter `node`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRunNodeSteps, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRunNodeSteps, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineRunNodeSteps, `run` is not nullable")
      }

      if (!missing(`node`) && is.null(`node`)) {
        stop("Invalid value for `node` when calling BlueOceanApi$GetPipelineRunNodeSteps, `node` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`node`)) {
        local_var_url_path <- gsub("\\{node\\}", URLencode(as.character(`node`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[PipelineStepImpl]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[PipelineRunNode]
    GetPipelineRunNodes = function(organization, pipeline, run, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunNodesWithHttpInfo(organization, pipeline, run, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[PipelineRunNode]) with additional information such as HTTP status code, headers
    GetPipelineRunNodesWithHttpInfo = function(organization, pipeline, run, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRunNodes, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRunNodes, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$GetPipelineRunNodes, `run` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[PipelineRunNode]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[PipelineRun]
    GetPipelineRuns = function(organization, pipeline, data_file = NULL, ...) {
      local_var_response <- self$GetPipelineRunsWithHttpInfo(organization, pipeline, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[PipelineRun]) with additional information such as HTTP status code, headers
    GetPipelineRunsWithHttpInfo = function(organization, pipeline, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelineRuns, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$GetPipelineRuns, `pipeline` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[PipelineRun]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[Pipeline]
    GetPipelines = function(organization, data_file = NULL, ...) {
      local_var_response <- self$GetPipelinesWithHttpInfo(organization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[Pipeline]) with additional information such as HTTP status code, headers
    GetPipelinesWithHttpInfo = function(organization, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetPipelines, `organization` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[Pipeline]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GithubScm
    GetSCM = function(organization, scm, data_file = NULL, ...) {
      local_var_response <- self$GetSCMWithHttpInfo(organization, scm, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GithubScm) with additional information such as HTTP status code, headers
    GetSCMWithHttpInfo = function(organization, scm, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`scm`)) {
        stop("Missing required parameter `scm`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetSCM, `organization` is not nullable")
      }

      if (!missing(`scm`) && is.null(`scm`)) {
        stop("Invalid value for `scm` when calling BlueOceanApi$GetSCM, `scm` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/scm/{scm}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scm`)) {
        local_var_url_path <- gsub("\\{scm\\}", URLencode(as.character(`scm`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GithubScm"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param scm_organisation Name of the SCM organization
    #' @param credential_id (optional) Credential ID
    #' @param page_size (optional) Number of items in a page
    #' @param page_number (optional) Page number
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[GithubOrganization]
    GetSCMOrganisationRepositories = function(organization, scm, scm_organisation, credential_id = NULL, page_size = NULL, page_number = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetSCMOrganisationRepositoriesWithHttpInfo(organization, scm, scm_organisation, credential_id, page_size, page_number, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param scm_organisation Name of the SCM organization
    #' @param credential_id (optional) Credential ID
    #' @param page_size (optional) Number of items in a page
    #' @param page_number (optional) Page number
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[GithubOrganization]) with additional information such as HTTP status code, headers
    GetSCMOrganisationRepositoriesWithHttpInfo = function(organization, scm, scm_organisation, credential_id = NULL, page_size = NULL, page_number = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`scm`)) {
        stop("Missing required parameter `scm`.")
      }

      if (missing(`scm_organisation`)) {
        stop("Missing required parameter `scm_organisation`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetSCMOrganisationRepositories, `organization` is not nullable")
      }

      if (!missing(`scm`) && is.null(`scm`)) {
        stop("Invalid value for `scm` when calling BlueOceanApi$GetSCMOrganisationRepositories, `scm` is not nullable")
      }

      if (!missing(`scm_organisation`) && is.null(`scm_organisation`)) {
        stop("Invalid value for `scm_organisation` when calling BlueOceanApi$GetSCMOrganisationRepositories, `scm_organisation` is not nullable")
      }

      if (!missing(`credential_id`) && is.null(`credential_id`)) {
        stop("Invalid value for `credential_id` when calling BlueOceanApi$GetSCMOrganisationRepositories, `credential_id` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling BlueOceanApi$GetSCMOrganisationRepositories, `page_size` is not nullable")
      }

      if (!missing(`page_number`) && is.null(`page_number`)) {
        stop("Invalid value for `page_number` when calling BlueOceanApi$GetSCMOrganisationRepositories, `page_number` is not nullable")
      }

      query_params[["credentialId"]] <- `credential_id`

      query_params[["pageSize"]] <- `page_size`

      query_params[["pageNumber"]] <- `page_number`

      local_var_url_path <- "/blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scm`)) {
        local_var_url_path <- gsub("\\{scm\\}", URLencode(as.character(`scm`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scm_organisation`)) {
        local_var_url_path <- gsub("\\{scmOrganisation\\}", URLencode(as.character(`scm_organisation`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[GithubOrganization]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param scm_organisation Name of the SCM organization
    #' @param repository Name of the SCM repository
    #' @param credential_id (optional) Credential ID
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[GithubOrganization]
    GetSCMOrganisationRepository = function(organization, scm, scm_organisation, repository, credential_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetSCMOrganisationRepositoryWithHttpInfo(organization, scm, scm_organisation, repository, credential_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param scm_organisation Name of the SCM organization
    #' @param repository Name of the SCM repository
    #' @param credential_id (optional) Credential ID
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[GithubOrganization]) with additional information such as HTTP status code, headers
    GetSCMOrganisationRepositoryWithHttpInfo = function(organization, scm, scm_organisation, repository, credential_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`scm`)) {
        stop("Missing required parameter `scm`.")
      }

      if (missing(`scm_organisation`)) {
        stop("Missing required parameter `scm_organisation`.")
      }

      if (missing(`repository`)) {
        stop("Missing required parameter `repository`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetSCMOrganisationRepository, `organization` is not nullable")
      }

      if (!missing(`scm`) && is.null(`scm`)) {
        stop("Invalid value for `scm` when calling BlueOceanApi$GetSCMOrganisationRepository, `scm` is not nullable")
      }

      if (!missing(`scm_organisation`) && is.null(`scm_organisation`)) {
        stop("Invalid value for `scm_organisation` when calling BlueOceanApi$GetSCMOrganisationRepository, `scm_organisation` is not nullable")
      }

      if (!missing(`repository`) && is.null(`repository`)) {
        stop("Invalid value for `repository` when calling BlueOceanApi$GetSCMOrganisationRepository, `repository` is not nullable")
      }

      if (!missing(`credential_id`) && is.null(`credential_id`)) {
        stop("Invalid value for `credential_id` when calling BlueOceanApi$GetSCMOrganisationRepository, `credential_id` is not nullable")
      }

      query_params[["credentialId"]] <- `credential_id`

      local_var_url_path <- "/blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories/{repository}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scm`)) {
        local_var_url_path <- gsub("\\{scm\\}", URLencode(as.character(`scm`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scm_organisation`)) {
        local_var_url_path <- gsub("\\{scmOrganisation\\}", URLencode(as.character(`scm_organisation`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`repository`)) {
        local_var_url_path <- gsub("\\{repository\\}", URLencode(as.character(`repository`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[GithubOrganization]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param credential_id (optional) Credential ID
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[GithubOrganization]
    GetSCMOrganisations = function(organization, scm, credential_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetSCMOrganisationsWithHttpInfo(organization, scm, credential_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param scm Name of SCM
    #' @param credential_id (optional) Credential ID
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[GithubOrganization]) with additional information such as HTTP status code, headers
    GetSCMOrganisationsWithHttpInfo = function(organization, scm, credential_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`scm`)) {
        stop("Missing required parameter `scm`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetSCMOrganisations, `organization` is not nullable")
      }

      if (!missing(`scm`) && is.null(`scm`)) {
        stop("Invalid value for `scm` when calling BlueOceanApi$GetSCMOrganisations, `scm` is not nullable")
      }

      if (!missing(`credential_id`) && is.null(`credential_id`)) {
        stop("Invalid value for `credential_id` when calling BlueOceanApi$GetSCMOrganisations, `credential_id` is not nullable")
      }

      query_params[["credentialId"]] <- `credential_id`

      local_var_url_path <- "/blue/rest/organizations/{organization}/scm/{scm}/organizations"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`scm`)) {
        local_var_url_path <- gsub("\\{scm\\}", URLencode(as.character(`scm`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[GithubOrganization]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param user Name of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return User
    GetUser = function(organization, user, data_file = NULL, ...) {
      local_var_response <- self$GetUserWithHttpInfo(organization, user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param user Name of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (User) with additional information such as HTTP status code, headers
    GetUserWithHttpInfo = function(organization, user, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`user`)) {
        stop("Missing required parameter `user`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetUser, `organization` is not nullable")
      }

      if (!missing(`user`) && is.null(`user`)) {
        stop("Invalid value for `user` when calling BlueOceanApi$GetUser, `user` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/users/{user}"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`user`)) {
        local_var_url_path <- gsub("\\{user\\}", URLencode(as.character(`user`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "User"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param user Name of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[FavoriteImpl]
    GetUserFavorites = function(user, data_file = NULL, ...) {
      local_var_response <- self$GetUserFavoritesWithHttpInfo(user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param user Name of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[FavoriteImpl]) with additional information such as HTTP status code, headers
    GetUserFavoritesWithHttpInfo = function(user, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`user`)) {
        stop("Missing required parameter `user`.")
      }

      if (!missing(`user`) && is.null(`user`)) {
        stop("Invalid value for `user` when calling BlueOceanApi$GetUserFavorites, `user` is not nullable")
      }

      local_var_url_path <- "/blue/rest/users/{user}/favorites"
      if (!missing(`user`)) {
        local_var_url_path <- gsub("\\{user\\}", URLencode(as.character(`user`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[FavoriteImpl]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return User
    GetUsers = function(organization, data_file = NULL, ...) {
      local_var_response <- self$GetUsersWithHttpInfo(organization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (User) with additional information such as HTTP status code, headers
    GetUsersWithHttpInfo = function(organization, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$GetUsers, `organization` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/users/"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "User"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QueueItemImpl
    PostPipelineRun = function(organization, pipeline, run, data_file = NULL, ...) {
      local_var_response <- self$PostPipelineRunWithHttpInfo(organization, pipeline, run, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QueueItemImpl) with additional information such as HTTP status code, headers
    PostPipelineRunWithHttpInfo = function(organization, pipeline, run, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$PostPipelineRun, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$PostPipelineRun, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$PostPipelineRun, `run` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/replay"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueItemImpl"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QueueItemImpl
    PostPipelineRuns = function(organization, pipeline, data_file = NULL, ...) {
      local_var_response <- self$PostPipelineRunsWithHttpInfo(organization, pipeline, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QueueItemImpl) with additional information such as HTTP status code, headers
    PostPipelineRunsWithHttpInfo = function(organization, pipeline, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$PostPipelineRuns, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$PostPipelineRuns, `pipeline` is not nullable")
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueItemImpl"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param body Set JSON string body to {\"favorite\": true} to favorite, set value to false to unfavorite
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return FavoriteImpl
    PutPipelineFavorite = function(organization, pipeline, body, data_file = NULL, ...) {
      local_var_response <- self$PutPipelineFavoriteWithHttpInfo(organization, pipeline, body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param body Set JSON string body to {\"favorite\": true} to favorite, set value to false to unfavorite
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (FavoriteImpl) with additional information such as HTTP status code, headers
    PutPipelineFavoriteWithHttpInfo = function(organization, pipeline, body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$PutPipelineFavorite, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$PutPipelineFavorite, `pipeline` is not nullable")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling BlueOceanApi$PutPipelineFavorite, `body` is not nullable")
      }

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/favorite"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "FavoriteImpl"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param blocking (optional) Set to true to make blocking stop, default: false
    #' @param time_out_in_secs (optional) Timeout in seconds, default: 10 seconds
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PipelineRun
    PutPipelineRun = function(organization, pipeline, run, blocking = NULL, time_out_in_secs = NULL, data_file = NULL, ...) {
      local_var_response <- self$PutPipelineRunWithHttpInfo(organization, pipeline, run, blocking, time_out_in_secs, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param organization Name of the organization
    #' @param pipeline Name of the pipeline
    #' @param run Name of the run
    #' @param blocking (optional) Set to true to make blocking stop, default: false
    #' @param time_out_in_secs (optional) Timeout in seconds, default: 10 seconds
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PipelineRun) with additional information such as HTTP status code, headers
    PutPipelineRunWithHttpInfo = function(organization, pipeline, run, blocking = NULL, time_out_in_secs = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (missing(`pipeline`)) {
        stop("Missing required parameter `pipeline`.")
      }

      if (missing(`run`)) {
        stop("Missing required parameter `run`.")
      }

      if (!missing(`organization`) && is.null(`organization`)) {
        stop("Invalid value for `organization` when calling BlueOceanApi$PutPipelineRun, `organization` is not nullable")
      }

      if (!missing(`pipeline`) && is.null(`pipeline`)) {
        stop("Invalid value for `pipeline` when calling BlueOceanApi$PutPipelineRun, `pipeline` is not nullable")
      }

      if (!missing(`run`) && is.null(`run`)) {
        stop("Invalid value for `run` when calling BlueOceanApi$PutPipelineRun, `run` is not nullable")
      }

      if (!missing(`blocking`) && is.null(`blocking`)) {
        stop("Invalid value for `blocking` when calling BlueOceanApi$PutPipelineRun, `blocking` is not nullable")
      }

      if (!missing(`time_out_in_secs`) && is.null(`time_out_in_secs`)) {
        stop("Invalid value for `time_out_in_secs` when calling BlueOceanApi$PutPipelineRun, `time_out_in_secs` is not nullable")
      }

      query_params[["blocking"]] <- `blocking`

      query_params[["timeOutInSecs"]] <- `time_out_in_secs`

      local_var_url_path <- "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/stop"
      if (!missing(`organization`)) {
        local_var_url_path <- gsub("\\{organization\\}", URLencode(as.character(`organization`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`pipeline`)) {
        local_var_url_path <- gsub("\\{pipeline\\}", URLencode(as.character(`pipeline`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run`)) {
        local_var_url_path <- gsub("\\{run\\}", URLencode(as.character(`run`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PipelineRun"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param q Query string
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return character
    Search = function(q, data_file = NULL, ...) {
      local_var_response <- self$SearchWithHttpInfo(q, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param q Query string
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    SearchWithHttpInfo = function(q, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`q`)) {
        stop("Missing required parameter `q`.")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling BlueOceanApi$Search, `q` is not nullable")
      }

      query_params[["q"]] <- `q`

      local_var_url_path <- "/blue/rest/search/"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param q Query string containing an array of class names
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return character
    SearchClasses = function(q, data_file = NULL, ...) {
      local_var_response <- self$SearchClassesWithHttpInfo(q, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param q Query string containing an array of class names
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    SearchClassesWithHttpInfo = function(q, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`q`)) {
        stop("Missing required parameter `q`.")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling BlueOceanApi$SearchClasses, `q` is not nullable")
      }

      query_params[["q"]] <- `q`

      local_var_url_path <- "/blue/rest/classes/"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
