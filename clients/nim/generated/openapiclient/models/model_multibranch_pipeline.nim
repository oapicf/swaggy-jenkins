#
# Swaggy Jenkins
# 
# Jenkins API clients generated from Swagger / Open API specification
# The version of the OpenAPI document: 3.2.1-pre.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type MultibranchPipeline* = object
  ## 
  displayName*: Option[string]
  estimatedDurationInMillis*: Option[int]
  latestRun*: Option[string]
  name*: Option[string]
  organization*: Option[string]
  weatherScore*: Option[int]
  branchNames*: Option[seq[string]]
  numberOfFailingBranches*: Option[int]
  numberOfFailingPullRequests*: Option[int]
  numberOfSuccessfulBranches*: Option[int]
  numberOfSuccessfulPullRequests*: Option[int]
  totalNumberOfBranches*: Option[int]
  totalNumberOfPullRequests*: Option[int]
  class*: Option[string]


# Custom JSON deserialization for MultibranchPipeline with custom field names
proc to*(node: JsonNode, T: typedesc[MultibranchPipeline]): MultibranchPipeline =
  result = MultibranchPipeline()
  if node.kind == JObject:
    if node.hasKey("displayName") and node["displayName"].kind != JNull:
      result.displayName = some(to(node["displayName"], typeof(result.displayName.get())))
    if node.hasKey("estimatedDurationInMillis") and node["estimatedDurationInMillis"].kind != JNull:
      result.estimatedDurationInMillis = some(to(node["estimatedDurationInMillis"], typeof(result.estimatedDurationInMillis.get())))
    if node.hasKey("latestRun") and node["latestRun"].kind != JNull:
      result.latestRun = some(to(node["latestRun"], typeof(result.latestRun.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("organization") and node["organization"].kind != JNull:
      result.organization = some(to(node["organization"], typeof(result.organization.get())))
    if node.hasKey("weatherScore") and node["weatherScore"].kind != JNull:
      result.weatherScore = some(to(node["weatherScore"], typeof(result.weatherScore.get())))
    if node.hasKey("branchNames") and node["branchNames"].kind != JNull:
      result.branchNames = some(to(node["branchNames"], typeof(result.branchNames.get())))
    if node.hasKey("numberOfFailingBranches") and node["numberOfFailingBranches"].kind != JNull:
      result.numberOfFailingBranches = some(to(node["numberOfFailingBranches"], typeof(result.numberOfFailingBranches.get())))
    if node.hasKey("numberOfFailingPullRequests") and node["numberOfFailingPullRequests"].kind != JNull:
      result.numberOfFailingPullRequests = some(to(node["numberOfFailingPullRequests"], typeof(result.numberOfFailingPullRequests.get())))
    if node.hasKey("numberOfSuccessfulBranches") and node["numberOfSuccessfulBranches"].kind != JNull:
      result.numberOfSuccessfulBranches = some(to(node["numberOfSuccessfulBranches"], typeof(result.numberOfSuccessfulBranches.get())))
    if node.hasKey("numberOfSuccessfulPullRequests") and node["numberOfSuccessfulPullRequests"].kind != JNull:
      result.numberOfSuccessfulPullRequests = some(to(node["numberOfSuccessfulPullRequests"], typeof(result.numberOfSuccessfulPullRequests.get())))
    if node.hasKey("totalNumberOfBranches") and node["totalNumberOfBranches"].kind != JNull:
      result.totalNumberOfBranches = some(to(node["totalNumberOfBranches"], typeof(result.totalNumberOfBranches.get())))
    if node.hasKey("totalNumberOfPullRequests") and node["totalNumberOfPullRequests"].kind != JNull:
      result.totalNumberOfPullRequests = some(to(node["totalNumberOfPullRequests"], typeof(result.totalNumberOfPullRequests.get())))
    if node.hasKey("_class") and node["_class"].kind != JNull:
      result.class = some(to(node["_class"], typeof(result.class.get())))

# Custom JSON serialization for MultibranchPipeline with custom field names
proc `%`*(obj: MultibranchPipeline): JsonNode =
  result = newJObject()
  if obj.displayName.isSome():
    result["displayName"] = %obj.displayName.get()
  if obj.estimatedDurationInMillis.isSome():
    result["estimatedDurationInMillis"] = %obj.estimatedDurationInMillis.get()
  if obj.latestRun.isSome():
    result["latestRun"] = %obj.latestRun.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.organization.isSome():
    result["organization"] = %obj.organization.get()
  if obj.weatherScore.isSome():
    result["weatherScore"] = %obj.weatherScore.get()
  if obj.branchNames.isSome():
    result["branchNames"] = %obj.branchNames.get()
  if obj.numberOfFailingBranches.isSome():
    result["numberOfFailingBranches"] = %obj.numberOfFailingBranches.get()
  if obj.numberOfFailingPullRequests.isSome():
    result["numberOfFailingPullRequests"] = %obj.numberOfFailingPullRequests.get()
  if obj.numberOfSuccessfulBranches.isSome():
    result["numberOfSuccessfulBranches"] = %obj.numberOfSuccessfulBranches.get()
  if obj.numberOfSuccessfulPullRequests.isSome():
    result["numberOfSuccessfulPullRequests"] = %obj.numberOfSuccessfulPullRequests.get()
  if obj.totalNumberOfBranches.isSome():
    result["totalNumberOfBranches"] = %obj.totalNumberOfBranches.get()
  if obj.totalNumberOfPullRequests.isSome():
    result["totalNumberOfPullRequests"] = %obj.totalNumberOfPullRequests.get()
  if obj.class.isSome():
    result["_class"] = %obj.class.get()

