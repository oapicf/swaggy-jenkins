#
# Swaggy Jenkins
# 
# Jenkins API clients generated from Swagger / Open API specification
# The version of the OpenAPI document: 3.2.1-pre.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_github_repositorieslinks
import model_github_repository

type GithubRepositories* = object
  ## 
  class*: Option[string]
  links*: Option[GithubRepositorieslinks]
  items*: Option[seq[GithubRepository]]
  lastPage*: Option[int]
  nextPage*: Option[int]
  pageSize*: Option[int]


# Custom JSON deserialization for GithubRepositories with custom field names
proc to*(node: JsonNode, T: typedesc[GithubRepositories]): GithubRepositories =
  result = GithubRepositories()
  if node.kind == JObject:
    if node.hasKey("_class") and node["_class"].kind != JNull:
      result.class = some(to(node["_class"], typeof(result.class.get())))
    if node.hasKey("_links") and node["_links"].kind != JNull:
      result.links = some(to(node["_links"], typeof(result.links.get())))
    if node.hasKey("items") and node["items"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        var arr: seq[GithubRepository] = @[]
        for item in arrayNode.items:
          arr.add(to(item, GithubRepository))
        result.items = some(arr)
    if node.hasKey("lastPage") and node["lastPage"].kind != JNull:
      result.lastPage = some(to(node["lastPage"], typeof(result.lastPage.get())))
    if node.hasKey("nextPage") and node["nextPage"].kind != JNull:
      result.nextPage = some(to(node["nextPage"], typeof(result.nextPage.get())))
    if node.hasKey("pageSize") and node["pageSize"].kind != JNull:
      result.pageSize = some(to(node["pageSize"], typeof(result.pageSize.get())))

# Custom JSON serialization for GithubRepositories with custom field names
proc `%`*(obj: GithubRepositories): JsonNode =
  result = newJObject()
  if obj.class.isSome():
    result["_class"] = %obj.class.get()
  if obj.links.isSome():
    result["_links"] = %obj.links.get()
  if obj.items.isSome():
    result["items"] = %obj.items.get()
  if obj.lastPage.isSome():
    result["lastPage"] = %obj.lastPage.get()
  if obj.nextPage.isSome():
    result["nextPage"] = %obj.nextPage.get()
  if obj.pageSize.isSome():
    result["pageSize"] = %obj.pageSize.get()

