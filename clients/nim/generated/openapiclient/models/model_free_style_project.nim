#
# Swaggy Jenkins
# 
# Jenkins API clients generated from Swagger / Open API specification
# The version of the OpenAPI document: 3.2.1-pre.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_free_style_build
import model_free_style_projectactions
import model_free_style_projecthealth_report
import model_null_scm

type FreeStyleProject* = object
  ## 
  class*: Option[string]
  name*: Option[string]
  url*: Option[string]
  color*: Option[string]
  actions*: Option[seq[FreeStyleProjectactions]]
  description*: Option[string]
  displayName*: Option[string]
  displayNameOrNull*: Option[string]
  fullDisplayName*: Option[string]
  fullName*: Option[string]
  buildable*: Option[bool]
  builds*: Option[seq[FreeStyleBuild]]
  firstBuild*: Option[FreeStyleBuild]
  healthReport*: Option[seq[FreeStyleProjecthealthReport]]
  inQueue*: Option[bool]
  keepDependencies*: Option[bool]
  lastBuild*: Option[FreeStyleBuild]
  lastCompletedBuild*: Option[FreeStyleBuild]
  lastFailedBuild*: Option[string]
  lastStableBuild*: Option[FreeStyleBuild]
  lastSuccessfulBuild*: Option[FreeStyleBuild]
  lastUnstableBuild*: Option[string]
  lastUnsuccessfulBuild*: Option[string]
  nextBuildNumber*: Option[int]
  queueItem*: Option[string]
  concurrentBuild*: Option[bool]
  scm*: Option[NullSCM]


# Custom JSON deserialization for FreeStyleProject with custom field names
proc to*(node: JsonNode, T: typedesc[FreeStyleProject]): FreeStyleProject =
  result = FreeStyleProject()
  if node.kind == JObject:
    if node.hasKey("_class") and node["_class"].kind != JNull:
      result.class = some(to(node["_class"], typeof(result.class.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("url") and node["url"].kind != JNull:
      result.url = some(to(node["url"], typeof(result.url.get())))
    if node.hasKey("color") and node["color"].kind != JNull:
      result.color = some(to(node["color"], typeof(result.color.get())))
    if node.hasKey("actions") and node["actions"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["actions"]
      if arrayNode.kind == JArray:
        var arr: seq[FreeStyleProjectactions] = @[]
        for item in arrayNode.items:
          arr.add(to(item, FreeStyleProjectactions))
        result.actions = some(arr)
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("displayName") and node["displayName"].kind != JNull:
      result.displayName = some(to(node["displayName"], typeof(result.displayName.get())))
    if node.hasKey("displayNameOrNull") and node["displayNameOrNull"].kind != JNull:
      result.displayNameOrNull = some(to(node["displayNameOrNull"], typeof(result.displayNameOrNull.get())))
    if node.hasKey("fullDisplayName") and node["fullDisplayName"].kind != JNull:
      result.fullDisplayName = some(to(node["fullDisplayName"], typeof(result.fullDisplayName.get())))
    if node.hasKey("fullName") and node["fullName"].kind != JNull:
      result.fullName = some(to(node["fullName"], typeof(result.fullName.get())))
    if node.hasKey("buildable") and node["buildable"].kind != JNull:
      result.buildable = some(to(node["buildable"], typeof(result.buildable.get())))
    if node.hasKey("builds") and node["builds"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["builds"]
      if arrayNode.kind == JArray:
        var arr: seq[FreeStyleBuild] = @[]
        for item in arrayNode.items:
          arr.add(to(item, FreeStyleBuild))
        result.builds = some(arr)
    if node.hasKey("firstBuild") and node["firstBuild"].kind != JNull:
      result.firstBuild = some(to(node["firstBuild"], typeof(result.firstBuild.get())))
    if node.hasKey("healthReport") and node["healthReport"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["healthReport"]
      if arrayNode.kind == JArray:
        var arr: seq[FreeStyleProjecthealthReport] = @[]
        for item in arrayNode.items:
          arr.add(to(item, FreeStyleProjecthealthReport))
        result.healthReport = some(arr)
    if node.hasKey("inQueue") and node["inQueue"].kind != JNull:
      result.inQueue = some(to(node["inQueue"], typeof(result.inQueue.get())))
    if node.hasKey("keepDependencies") and node["keepDependencies"].kind != JNull:
      result.keepDependencies = some(to(node["keepDependencies"], typeof(result.keepDependencies.get())))
    if node.hasKey("lastBuild") and node["lastBuild"].kind != JNull:
      result.lastBuild = some(to(node["lastBuild"], typeof(result.lastBuild.get())))
    if node.hasKey("lastCompletedBuild") and node["lastCompletedBuild"].kind != JNull:
      result.lastCompletedBuild = some(to(node["lastCompletedBuild"], typeof(result.lastCompletedBuild.get())))
    if node.hasKey("lastFailedBuild") and node["lastFailedBuild"].kind != JNull:
      result.lastFailedBuild = some(to(node["lastFailedBuild"], typeof(result.lastFailedBuild.get())))
    if node.hasKey("lastStableBuild") and node["lastStableBuild"].kind != JNull:
      result.lastStableBuild = some(to(node["lastStableBuild"], typeof(result.lastStableBuild.get())))
    if node.hasKey("lastSuccessfulBuild") and node["lastSuccessfulBuild"].kind != JNull:
      result.lastSuccessfulBuild = some(to(node["lastSuccessfulBuild"], typeof(result.lastSuccessfulBuild.get())))
    if node.hasKey("lastUnstableBuild") and node["lastUnstableBuild"].kind != JNull:
      result.lastUnstableBuild = some(to(node["lastUnstableBuild"], typeof(result.lastUnstableBuild.get())))
    if node.hasKey("lastUnsuccessfulBuild") and node["lastUnsuccessfulBuild"].kind != JNull:
      result.lastUnsuccessfulBuild = some(to(node["lastUnsuccessfulBuild"], typeof(result.lastUnsuccessfulBuild.get())))
    if node.hasKey("nextBuildNumber") and node["nextBuildNumber"].kind != JNull:
      result.nextBuildNumber = some(to(node["nextBuildNumber"], typeof(result.nextBuildNumber.get())))
    if node.hasKey("queueItem") and node["queueItem"].kind != JNull:
      result.queueItem = some(to(node["queueItem"], typeof(result.queueItem.get())))
    if node.hasKey("concurrentBuild") and node["concurrentBuild"].kind != JNull:
      result.concurrentBuild = some(to(node["concurrentBuild"], typeof(result.concurrentBuild.get())))
    if node.hasKey("scm") and node["scm"].kind != JNull:
      result.scm = some(to(node["scm"], typeof(result.scm.get())))

# Custom JSON serialization for FreeStyleProject with custom field names
proc `%`*(obj: FreeStyleProject): JsonNode =
  result = newJObject()
  if obj.class.isSome():
    result["_class"] = %obj.class.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.url.isSome():
    result["url"] = %obj.url.get()
  if obj.color.isSome():
    result["color"] = %obj.color.get()
  if obj.actions.isSome():
    result["actions"] = %obj.actions.get()
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  if obj.displayName.isSome():
    result["displayName"] = %obj.displayName.get()
  if obj.displayNameOrNull.isSome():
    result["displayNameOrNull"] = %obj.displayNameOrNull.get()
  if obj.fullDisplayName.isSome():
    result["fullDisplayName"] = %obj.fullDisplayName.get()
  if obj.fullName.isSome():
    result["fullName"] = %obj.fullName.get()
  if obj.buildable.isSome():
    result["buildable"] = %obj.buildable.get()
  if obj.builds.isSome():
    result["builds"] = %obj.builds.get()
  if obj.firstBuild.isSome():
    result["firstBuild"] = %obj.firstBuild.get()
  if obj.healthReport.isSome():
    result["healthReport"] = %obj.healthReport.get()
  if obj.inQueue.isSome():
    result["inQueue"] = %obj.inQueue.get()
  if obj.keepDependencies.isSome():
    result["keepDependencies"] = %obj.keepDependencies.get()
  if obj.lastBuild.isSome():
    result["lastBuild"] = %obj.lastBuild.get()
  if obj.lastCompletedBuild.isSome():
    result["lastCompletedBuild"] = %obj.lastCompletedBuild.get()
  if obj.lastFailedBuild.isSome():
    result["lastFailedBuild"] = %obj.lastFailedBuild.get()
  if obj.lastStableBuild.isSome():
    result["lastStableBuild"] = %obj.lastStableBuild.get()
  if obj.lastSuccessfulBuild.isSome():
    result["lastSuccessfulBuild"] = %obj.lastSuccessfulBuild.get()
  if obj.lastUnstableBuild.isSome():
    result["lastUnstableBuild"] = %obj.lastUnstableBuild.get()
  if obj.lastUnsuccessfulBuild.isSome():
    result["lastUnsuccessfulBuild"] = %obj.lastUnsuccessfulBuild.get()
  if obj.nextBuildNumber.isSome():
    result["nextBuildNumber"] = %obj.nextBuildNumber.get()
  if obj.queueItem.isSome():
    result["queueItem"] = %obj.queueItem.get()
  if obj.concurrentBuild.isSome():
    result["concurrentBuild"] = %obj.concurrentBuild.get()
  if obj.scm.isSome():
    result["scm"] = %obj.scm.get()

