#
# Swaggy Jenkins
# 
# Jenkins API clients generated from Swagger / Open API specification
# The version of the OpenAPI document: 3.2.1-pre.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_queue_blocked_item

type Queue* = object
  ## 
  class*: Option[string]
  items*: Option[seq[QueueBlockedItem]]


# Custom JSON deserialization for Queue with custom field names
proc to*(node: JsonNode, T: typedesc[Queue]): Queue =
  result = Queue()
  if node.kind == JObject:
    if node.hasKey("_class") and node["_class"].kind != JNull:
      result.class = some(to(node["_class"], typeof(result.class.get())))
    if node.hasKey("items") and node["items"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        var arr: seq[QueueBlockedItem] = @[]
        for item in arrayNode.items:
          arr.add(to(item, QueueBlockedItem))
        result.items = some(arr)

# Custom JSON serialization for Queue with custom field names
proc `%`*(obj: Queue): JsonNode =
  result = newJObject()
  if obj.class.isSome():
    result["_class"] = %obj.class.get()
  if obj.items.isSome():
    result["items"] = %obj.items.get()

