#
# Swaggy Jenkins
# 
# Jenkins API clients generated from Swagger / Open API specification
# The version of the OpenAPI document: 3.2.1-pre.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_cause_user_id_cause

type CauseAction* = object
  ## 
  class*: Option[string]
  causes*: Option[seq[CauseUserIdCause]]


# Custom JSON deserialization for CauseAction with custom field names
proc to*(node: JsonNode, T: typedesc[CauseAction]): CauseAction =
  result = CauseAction()
  if node.kind == JObject:
    if node.hasKey("_class") and node["_class"].kind != JNull:
      result.class = some(to(node["_class"], typeof(result.class.get())))
    if node.hasKey("causes") and node["causes"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["causes"]
      if arrayNode.kind == JArray:
        var arr: seq[CauseUserIdCause] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CauseUserIdCause))
        result.causes = some(arr)

# Custom JSON serialization for CauseAction with custom field names
proc `%`*(obj: CauseAction): JsonNode =
  result = newJObject()
  if obj.class.isSome():
    result["_class"] = %obj.class.get()
  if obj.causes.isSome():
    result["causes"] = %obj.causes.get()

