#
# Swaggy Jenkins
# 
# Jenkins API clients generated from Swagger / Open API specification
# The version of the OpenAPI document: 3.2.1-pre.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type SwapSpaceMonitorMemoryUsage2* = object
  ## 
  class*: Option[string]
  availablePhysicalMemory*: Option[int]
  availableSwapSpace*: Option[int]
  totalPhysicalMemory*: Option[int]
  totalSwapSpace*: Option[int]


# Custom JSON deserialization for SwapSpaceMonitorMemoryUsage2 with custom field names
proc to*(node: JsonNode, T: typedesc[SwapSpaceMonitorMemoryUsage2]): SwapSpaceMonitorMemoryUsage2 =
  result = SwapSpaceMonitorMemoryUsage2()
  if node.kind == JObject:
    if node.hasKey("_class") and node["_class"].kind != JNull:
      result.class = some(to(node["_class"], typeof(result.class.get())))
    if node.hasKey("availablePhysicalMemory") and node["availablePhysicalMemory"].kind != JNull:
      result.availablePhysicalMemory = some(to(node["availablePhysicalMemory"], typeof(result.availablePhysicalMemory.get())))
    if node.hasKey("availableSwapSpace") and node["availableSwapSpace"].kind != JNull:
      result.availableSwapSpace = some(to(node["availableSwapSpace"], typeof(result.availableSwapSpace.get())))
    if node.hasKey("totalPhysicalMemory") and node["totalPhysicalMemory"].kind != JNull:
      result.totalPhysicalMemory = some(to(node["totalPhysicalMemory"], typeof(result.totalPhysicalMemory.get())))
    if node.hasKey("totalSwapSpace") and node["totalSwapSpace"].kind != JNull:
      result.totalSwapSpace = some(to(node["totalSwapSpace"], typeof(result.totalSwapSpace.get())))

# Custom JSON serialization for SwapSpaceMonitorMemoryUsage2 with custom field names
proc `%`*(obj: SwapSpaceMonitorMemoryUsage2): JsonNode =
  result = newJObject()
  if obj.class.isSome():
    result["_class"] = %obj.class.get()
  if obj.availablePhysicalMemory.isSome():
    result["availablePhysicalMemory"] = %obj.availablePhysicalMemory.get()
  if obj.availableSwapSpace.isSome():
    result["availableSwapSpace"] = %obj.availableSwapSpace.get()
  if obj.totalPhysicalMemory.isSome():
    result["totalPhysicalMemory"] = %obj.totalPhysicalMemory.get()
  if obj.totalSwapSpace.isSome():
    result["totalSwapSpace"] = %obj.totalSwapSpace.get()

