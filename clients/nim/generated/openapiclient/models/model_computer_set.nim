#
# Swaggy Jenkins
# 
# Jenkins API clients generated from Swagger / Open API specification
# The version of the OpenAPI document: 3.2.1-pre.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_hudson_master_computer

type ComputerSet* = object
  ## 
  class*: Option[string]
  busyExecutors*: Option[int]
  computer*: Option[seq[HudsonMasterComputer]]
  displayName*: Option[string]
  totalExecutors*: Option[int]


# Custom JSON deserialization for ComputerSet with custom field names
proc to*(node: JsonNode, T: typedesc[ComputerSet]): ComputerSet =
  result = ComputerSet()
  if node.kind == JObject:
    if node.hasKey("_class") and node["_class"].kind != JNull:
      result.class = some(to(node["_class"], typeof(result.class.get())))
    if node.hasKey("busyExecutors") and node["busyExecutors"].kind != JNull:
      result.busyExecutors = some(to(node["busyExecutors"], typeof(result.busyExecutors.get())))
    if node.hasKey("computer") and node["computer"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["computer"]
      if arrayNode.kind == JArray:
        var arr: seq[HudsonMasterComputer] = @[]
        for item in arrayNode.items:
          arr.add(to(item, HudsonMasterComputer))
        result.computer = some(arr)
    if node.hasKey("displayName") and node["displayName"].kind != JNull:
      result.displayName = some(to(node["displayName"], typeof(result.displayName.get())))
    if node.hasKey("totalExecutors") and node["totalExecutors"].kind != JNull:
      result.totalExecutors = some(to(node["totalExecutors"], typeof(result.totalExecutors.get())))

# Custom JSON serialization for ComputerSet with custom field names
proc `%`*(obj: ComputerSet): JsonNode =
  result = newJObject()
  if obj.class.isSome():
    result["_class"] = %obj.class.get()
  if obj.busyExecutors.isSome():
    result["busyExecutors"] = %obj.busyExecutors.get()
  if obj.computer.isSome():
    result["computer"] = %obj.computer.get()
  if obj.displayName.isSome():
    result["displayName"] = %obj.displayName.get()
  if obj.totalExecutors.isSome():
    result["totalExecutors"] = %obj.totalExecutors.get()

