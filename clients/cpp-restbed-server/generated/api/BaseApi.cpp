/**
 * Swaggy Jenkins
 * Jenkins API clients generated from Swagger / Open API specification
 *
 * The version of the OpenAPI document: 1.1.1
 * Contact: blah@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 5.4.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "BaseApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

BaseApiException::BaseApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int BaseApiException::getStatus() const
{
    return m_status;
}
const char* BaseApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

BaseApiCrumbIssuerApiJsonResource::BaseApiCrumbIssuerApiJsonResource(const std::string& context /* = "" */)
{
	this->set_path(context + "/crumbIssuer/api/json/");
	this->set_method_handler("GET",
		std::bind(&BaseApiCrumbIssuerApiJsonResource::handler_GET_internal, this,
			std::placeholders::_1));
}

BaseApiCrumbIssuerApiJsonResource::~BaseApiCrumbIssuerApiJsonResource()
{
}

std::pair<int, std::string> BaseApiCrumbIssuerApiJsonResource::handleBaseApiException(const BaseApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BaseApiCrumbIssuerApiJsonResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BaseApiCrumbIssuerApiJsonResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BaseApiCrumbIssuerApiJsonResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BaseApiCrumbIssuerApiJsonResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BaseApiCrumbIssuerApiJsonResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BaseApiCrumbIssuerApiJsonResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();




    int status_code = 500;
    std::shared_ptr<DefaultCrumbIssuer> resultObject = std::make_shared<DefaultCrumbIssuer>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET();
    }
    catch(const BaseApiException& e) {
        std::tie(status_code, result) = handleBaseApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "Successfully retrieved CSRF protection token" : result, contentType);
        return;
    }
    if (status_code == 401) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 401, result.empty() ? "Authentication failed - incorrect username and/or password" : result, contentType);
        return;
    }
    if (status_code == 403) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 403, result.empty() ? "Jenkins requires authentication - please set username and password" : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<DefaultCrumbIssuer>> BaseApiCrumbIssuerApiJsonResource::handler_GET(
        )
{
    throw BaseApiException(501, "Not implemented");
}


std::string BaseApiCrumbIssuerApiJsonResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

BaseApi::BaseApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

BaseApi::~BaseApi() {}

void BaseApi::setBaseApiCrumbIssuerApiJsonResource(std::shared_ptr<BaseApiCrumbIssuerApiJsonResource> spBaseApiCrumbIssuerApiJsonResource) {
    m_spBaseApiCrumbIssuerApiJsonResource = spBaseApiCrumbIssuerApiJsonResource;
    m_service->publish(m_spBaseApiCrumbIssuerApiJsonResource);
}


void BaseApi::publishDefaultResources() {
    if (!m_spBaseApiCrumbIssuerApiJsonResource) {
        setBaseApiCrumbIssuerApiJsonResource(std::make_shared<BaseApiCrumbIssuerApiJsonResource>());
    }
}

std::shared_ptr<restbed::Service> BaseApi::service() {
    return m_service;
}


}
}
}
}

