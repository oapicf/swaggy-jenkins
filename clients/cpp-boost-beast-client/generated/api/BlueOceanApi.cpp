/**
 * Swaggy Jenkins
 * Jenkins API clients generated from Swagger / Open API specification
 *
 * The version of the OpenAPI document: 3.4.1-pre.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <sstream>
#include <stdexcept>
#include <string>
#include <type_traits>
#include <map>
#include <array>
#include <algorithm>
#include <utility>
#include <vector>

#include <boost/lexical_cast.hpp>
#include <boost/beast/http/status.hpp>
#include <boost/version.hpp>
#include <boost/beast/core/detail/base64.hpp>
#include <boost/algorithm/string.hpp>
#include <boost/json.hpp>


#include "BlueOceanApi.h"


namespace org {
namespace openapitools {
namespace client {
namespace api {

using namespace org::openapitools::client::model;


namespace {
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 1> preferredTypes = {"json"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
                                      contentTypes.cend(),
                                      [preferredType](const std::string& str) {
                                            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    if (contentTypes.size() == 0) {
        return "application/json";
    }

    return contentTypes.at(0);
}

inline std::string normalizeMediaType(const std::string& contentType) {
    const auto parameterSeparator = contentType.find(';');
    std::string mediaType = contentType.substr(0, parameterSeparator);
    boost::algorithm::trim(mediaType);
    boost::algorithm::to_lower(mediaType);
    return mediaType;
}

inline bool isJsonContentType(const std::string& contentType) {
    const std::string mediaType = normalizeMediaType(contentType);
    return mediaType == "application/json"
        || (mediaType.size() > 5 && mediaType.compare(mediaType.size() - 5, 5, "+json") == 0);
}
}

struct FormParameter {
    FormParameter(std::string parameterName, std::string parameterValue, bool file)
        : name(std::move(parameterName)), value(std::move(parameterValue)), isFile(file) {
    }

    std::string name;
    std::string value;
    bool isFile;
};

inline std::string toFormParameterValue(const std::string& value) {
    return value;
}

inline std::string toFormParameterValue(bool value) {
    return value ? "true" : "false";
}

inline std::string toRawBodyValue(const std::string& value) {
    return value;
}

inline std::string toRawBodyValue(bool value) {
    return value ? "true" : "false";
}

template<typename T>
std::string toRawBodyValue(const T& value) {
    return boost::lexical_cast<std::string>(value);
}

template<typename T>
std::string toFormParameterValue(const T& value) {
    return boost::lexical_cast<std::string>(value);
}

template<typename T>
std::string toFormParameterValue(const std::vector<T>& values) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const auto& value : values) {
        serializedValues << separator << toFormParameterValue(value);
        separator = ",";
    }
    return serializedValues.str();
}

inline std::string percentEncodeFormValue(const std::string& value) {
    static const char hexDigits[] = "0123456789ABCDEF";
    std::string encodedValue;
    encodedValue.reserve(value.size());
    for (const unsigned char character : value) {
        if ((character >= 'a' && character <= 'z')
            || (character >= 'A' && character <= 'Z')
            || (character >= '0' && character <= '9')
            || character == '-' || character == '.' || character == '_'
            || character == '~') {
            encodedValue.push_back(static_cast<char>(character));
        } else if (character == ' ') {
            encodedValue.push_back('+');
        } else {
            encodedValue.push_back('%');
            encodedValue.push_back(hexDigits[(character >> 4) & 0x0F]);
            encodedValue.push_back(hexDigits[character & 0x0F]);
        }
    }
    return encodedValue;
}

inline std::string percentEncodeRfc3986Value(const std::string& unencodedValue) {
    static const char hexDigits[] = "0123456789ABCDEF";
    std::string encodedValue;
    encodedValue.reserve(unencodedValue.size());
    for (const unsigned char character : unencodedValue) {
        if ((character >= 'a' && character <= 'z')
            || (character >= 'A' && character <= 'Z')
            || (character >= '0' && character <= '9')
            || character == '-' || character == '.' || character == '_'
            || character == '~') {
            encodedValue.push_back(static_cast<char>(character));
        } else {
            encodedValue.push_back('%');
            encodedValue.push_back(hexDigits[(character >> 4) & 0x0F]);
            encodedValue.push_back(hexDigits[character & 0x0F]);
        }
    }
    return encodedValue;
}

inline std::string percentEncodePathValue(const std::string& unencodedValue) {
    return percentEncodeRfc3986Value(unencodedValue);
}

inline std::string percentEncodeQueryValue(const std::string& unencodedValue) {
    return percentEncodeRfc3986Value(unencodedValue);
}

template<typename T>
std::string serializePathParameterValue(const T& pathParameterValue) {
    return percentEncodePathValue(toFormParameterValue(pathParameterValue));
}

template<typename T>
std::string serializePathParameterValue(const std::vector<T>& pathParameterValues) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const T& pathParameterValue : pathParameterValues) {
        serializedValues << separator
                         << percentEncodePathValue(toFormParameterValue(pathParameterValue));
        separator = ",";
    }
    return serializedValues.str();
}

template<typename T>
void replacePathParameter(
    std::string& path,
    const std::string& parameterName,
    const T& parameterValue) {
    const std::string placeholder = "{" + parameterName + "}";
    const std::string serializedValue = serializePathParameterValue(parameterValue);
    std::string::size_type position = 0;
    while ((position = path.find(placeholder, position)) != std::string::npos) {
        path.replace(position, placeholder.size(), serializedValue);
        position += serializedValue.size();
    }
}

template<typename T>
std::string serializeQueryParameterValue(const T& queryParameterValue) {
    return percentEncodeQueryValue(toFormParameterValue(queryParameterValue));
}

template<typename T>
std::string serializeQueryParameterValue(
    const std::vector<T>& queryParameterValues,
    const std::string& collectionDelimiter) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const T& queryParameterValue : queryParameterValues) {
        serializedValues << separator
                         << percentEncodeQueryValue(toFormParameterValue(queryParameterValue));
        separator = collectionDelimiter.c_str();
    }
    return serializedValues.str();
}

template<typename T>
std::string serializeQueryParameterValue(
    const std::map<std::string, T>& queryParameterValues,
    const std::string& collectionDelimiter) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const auto& queryParameterValue : queryParameterValues) {
        serializedValues << separator
                         << percentEncodeQueryValue(queryParameterValue.first)
                         << collectionDelimiter
                         << percentEncodeQueryValue(toFormParameterValue(queryParameterValue.second));
        separator = collectionDelimiter.c_str();
    }
    return serializedValues.str();
}

inline void appendQueryParameter(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::string& parameterName,
    const std::string& parameterValue) {
    queryParameterStream << queryParameterSeparator
                         << percentEncodeQueryValue(parameterName) << '='
                         << parameterValue;
    queryParameterSeparator = "&";
}

template<typename T>
void appendMultiQueryParameters(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::string& parameterName,
    const std::vector<T>& queryParameterValues) {
    for (const T& queryParameterValue : queryParameterValues) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            parameterName,
            serializeQueryParameterValue(queryParameterValue));
    }
}

template<typename T>
void appendExplodedQueryParameters(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::map<std::string, T>& queryParameterValues) {
    for (const auto& queryParameterValue : queryParameterValues) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            queryParameterValue.first,
            serializeQueryParameterValue(queryParameterValue.second));
    }
}

template<typename T>
void appendDeepObjectQueryParameters(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::string& parameterName,
    const std::map<std::string, T>& queryParameterValues) {
    for (const auto& queryParameterValue : queryParameterValues) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            parameterName + "[" + queryParameterValue.first + "]",
            serializeQueryParameterValue(queryParameterValue.second));
    }
}

inline std::string serializeHeaderParameterValue(const std::string& headerParameterValue) {
    return headerParameterValue;
}

inline std::string serializeHeaderParameterValue(bool headerParameterValue) {
    return headerParameterValue ? "true" : "false";
}

template<typename T>
typename std::enable_if<std::is_arithmetic<T>::value, std::string>::type
serializeHeaderParameterValue(const T& headerParameterValue) {
    return boost::lexical_cast<std::string>(headerParameterValue);
}

template<typename T>
typename std::enable_if<!std::is_arithmetic<T>::value, std::string>::type
serializeHeaderParameterValue(const T&) {
    throw std::invalid_argument(
        "Header parameter serialization supports only primitive values and arrays of primitive values");
}

template<typename T>
std::string serializeHeaderParameterValue(const std::vector<T>& headerParameterValues) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const T& headerParameterValue : headerParameterValues) {
        serializedValues << separator << serializeHeaderParameterValue(headerParameterValue);
        separator = ",";
    }
    return serializedValues.str();
}

inline std::string serializeUrlEncodedFormData(const std::vector<FormParameter>& formParameters) {
    std::stringstream serializedFormData;
    const char* separator = "";
    for (const auto& formParameter : formParameters) {
        serializedFormData << separator
                           << percentEncodeFormValue(formParameter.name)
                           << '='
                           << percentEncodeFormValue(formParameter.value);
        separator = "&";
    }
    return serializedFormData.str();
}

inline std::string selectMultipartBoundary(const std::vector<FormParameter>& formParameters) {
    std::string boundary = "OpenAPIGeneratorBoundary";
    for (;;) {
        const auto collision = std::find_if(
            formParameters.cbegin(),
            formParameters.cend(),
            [&boundary](const FormParameter& formParameter) {
                return formParameter.value.find(boundary) != std::string::npos;
            });
        if (collision == formParameters.cend()) {
            return boundary;
        }
        boundary.push_back('X');
    }
}

inline std::string escapeMultipartParameter(const std::string& value) {
    std::string escapedValue;
    escapedValue.reserve(value.size());
    for (const char character : value) {
        if (character == '\\' || character == '"') {
            escapedValue.push_back('\\');
        }
        escapedValue.push_back(character);
    }
    return escapedValue;
}

inline std::string serializeMultipartFormData(
    const std::vector<FormParameter>& formParameters,
    const std::string& boundary) {
    std::stringstream serializedFormData;
    for (const auto& formParameter : formParameters) {
        serializedFormData << "--" << boundary << "\r\n"
                           << "Content-Disposition: form-data; name=\""
                           << escapeMultipartParameter(formParameter.name) << '"';
        if (formParameter.isFile) {
            serializedFormData << "; filename=\""
                               << escapeMultipartParameter(formParameter.name) << '"';
        }
        serializedFormData << "\r\n";
        if (formParameter.isFile) {
            serializedFormData << "Content-Type: application/octet-stream\r\n";
        }
        serializedFormData << "\r\n" << formParameter.value << "\r\n";
    }
    serializedFormData << "--" << boundary << "--\r\n";
    return serializedFormData.str();
}

namespace {

std::string base64encodeImpl(const std::string& str) {
#if BOOST_VERSION < 107100
    return boost::beast::detail::base64_encode(str);
#else
    std::string encoded;
    encoded.resize(boost::beast::detail::base64::encoded_size(str.size()));
    if (!str.empty()) {
        encoded.resize(boost::beast::detail::base64::encode(&encoded[0], str.data(), str.size()));
    }
    return encoded;
#endif
}

template<typename T>
boost::json::value toRequestJsonValue(const T& requestValue);

template<typename T>
boost::json::value toRequestJsonValue(const std::shared_ptr<T>& requestValue);

template<typename T>
boost::json::value toRequestJsonValue(const std::vector<T>& requestValues);

template<typename T>
boost::json::value toRequestJsonValue(const std::map<std::string, T>& requestValues);

template<typename T>
boost::json::value toRequestJsonValue(const T& requestValue) {
    return boost::json::value_from(requestValue);
}

template<typename T>
boost::json::value toRequestJsonValue(const std::shared_ptr<T>& requestValue) {
    return requestValue == nullptr ? boost::json::value(nullptr) : requestValue->toJsonValue();
}

template<typename T>
boost::json::value toRequestJsonValue(const std::vector<T>& requestValues) {
    boost::json::array requestArray;
    requestArray.reserve(requestValues.size());
    for (const auto& requestValue : requestValues) {
        requestArray.emplace_back(toRequestJsonValue(requestValue));
    }
    return requestArray;
}

template<typename T>
boost::json::value toRequestJsonValue(const std::map<std::string, T>& requestValues) {
    boost::json::object requestObject;
    for (const auto& requestEntry : requestValues) {
        requestObject.emplace(requestEntry.first, toRequestJsonValue(requestEntry.second));
    }
    return requestObject;
}

template<typename T>
struct ResponseJsonValueConverter {
    static T convert(const boost::json::value& responseValue) {
        return boost::json::value_to<T>(responseValue);
    }
};

template<>
struct ResponseJsonValueConverter<boost::json::value> {
    static boost::json::value convert(const boost::json::value& responseValue) {
        return responseValue;
    }
};

template<typename T>
struct ResponseJsonValueConverter<std::shared_ptr<T>> {
    static std::shared_ptr<T> convert(const boost::json::value& responseValue) {
        return responseValue.is_null() ? nullptr : std::make_shared<T>(responseValue);
    }
};

template<typename T>
struct ResponseJsonValueConverter<std::vector<T>> {
    static std::vector<T> convert(const boost::json::value& responseValue) {
        const auto& responseArray = responseValue.as_array();
        std::vector<T> convertedValues;
        convertedValues.reserve(responseArray.size());
        for (const auto& arrayValue : responseArray) {
            convertedValues.emplace_back(ResponseJsonValueConverter<T>::convert(arrayValue));
        }
        return convertedValues;
    }
};

template<typename T>
struct ResponseJsonValueConverter<std::map<std::string, T>> {
    static std::map<std::string, T> convert(const boost::json::value& responseValue) {
        const auto& responseObject = responseValue.as_object();
        std::map<std::string, T> convertedValues;
        for (const auto& objectEntry : responseObject) {
            convertedValues.emplace(
                objectEntry.key_c_str(),
                ResponseJsonValueConverter<T>::convert(objectEntry.value()));
        }
        return convertedValues;
    }
};

template<typename T>
struct ResponseBodyDeserializer {
    static void deserialize(
        T& convertedResponse,
        const std::string& responseBody,
        const std::string& responseContentType,
        bool tolerateEmptyBody) {
        if (!isJsonContentType(responseContentType)) {
            throw std::invalid_argument(
                "Content type '" + responseContentType + "' does not support structured response bodies");
        }
        if (tolerateEmptyBody && responseBody.empty()) {
            return;
        }
        convertedResponse = ResponseJsonValueConverter<T>::convert(boost::json::parse(responseBody));
    }
};

template<>
struct ResponseBodyDeserializer<std::string> {
    static void deserialize(
        std::string& convertedResponse,
        const std::string& responseBody,
        const std::string&,
        bool) {
        convertedResponse = responseBody;
    }
};
}

BlueOceanApiException::BlueOceanApiException(boost::beast::http::status statusCode, std::string what)
  : m_status(statusCode),
    m_what(what)
{
}

boost::beast::http::status BlueOceanApiException::getStatus() const
{
    return m_status;
}

const char* BlueOceanApiException::what() const noexcept
{
    return m_what.c_str();
}

void
BlueOceanApi::deletePipelineQueueItem(
    const std::string& organization, const std::string& pipeline, const std::string& queue) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/queue/{queue}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "queue", queue);


    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("DELETE",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    if (statusCode == boost::beast::http::status(200)) {
        return;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<User>
BlueOceanApi::getAuthenticatedUser(
    const std::string& organization) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/user";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<User> deserializedResponse = std::make_shared<User>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<User>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::string
BlueOceanApi::getClasses(
    const std::string& r_class) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/classes/{class}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "class", r_class);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::string deserializedResponse = "";
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::string>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::string
BlueOceanApi::getJsonWebKey(
    const int32_t& key) {
    std::string serializedRequestBody;
    std::string path = m_context + "/jwt-auth/jwks/{key}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "key", key);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::string deserializedResponse = "";
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::string>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::string
BlueOceanApi::getJsonWebToken(
    const boost::optional<int32_t>& expiryTimeInMins, const boost::optional<int32_t>& maxExpiryTimeInMins) {
    std::string serializedRequestBody;
    std::string path = m_context + "/jwt-auth/token";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (expiryTimeInMins) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "expiryTimeInMins",
            serializeQueryParameterValue(*expiryTimeInMins));
    }
    if (maxExpiryTimeInMins) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "maxExpiryTimeInMins",
            serializeQueryParameterValue(*maxExpiryTimeInMins));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::string deserializedResponse = "";
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::string>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<Organisation>
BlueOceanApi::getOrganisation(
    const std::string& organization) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<Organisation> deserializedResponse = std::make_shared<Organisation>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<Organisation>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw BlueOceanApiException(statusCode, "Pipeline cannot be found on Jenkins instance");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<Organisation>>
BlueOceanApi::getOrganisations(
    ) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations";
    std::map<std::string, std::string> headers;

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<Organisation>> deserializedResponse = std::vector<std::shared_ptr<Organisation>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<Organisation>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<Pipeline>
BlueOceanApi::getPipeline(
    const std::string& organization, const std::string& pipeline) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<Pipeline> deserializedResponse = std::make_shared<Pipeline>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<Pipeline>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw BlueOceanApiException(statusCode, "Pipeline cannot be found on Jenkins instance");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<PipelineActivity>>
BlueOceanApi::getPipelineActivities(
    const std::string& organization, const std::string& pipeline) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/activities";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<PipelineActivity>> deserializedResponse = std::vector<std::shared_ptr<PipelineActivity>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<PipelineActivity>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<BranchImpl>
BlueOceanApi::getPipelineBranch(
    const std::string& organization, const std::string& pipeline, const std::string& branch) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "branch", branch);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<BranchImpl> deserializedResponse = std::make_shared<BranchImpl>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<BranchImpl>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<PipelineRun>
BlueOceanApi::getPipelineBranchRun(
    const std::string& organization, const std::string& pipeline, const std::string& branch, const std::string& run) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches/{branch}/runs/{run}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "branch", branch);
    replacePathParameter(path, "run", run);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<PipelineRun> deserializedResponse = std::make_shared<PipelineRun>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<PipelineRun>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<MultibranchPipeline>
BlueOceanApi::getPipelineBranches(
    const std::string& organization, const std::string& pipeline) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/branches";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<MultibranchPipeline> deserializedResponse = std::make_shared<MultibranchPipeline>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<MultibranchPipeline>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<PipelineFolderImpl>
BlueOceanApi::getPipelineFolder(
    const std::string& organization, const std::string& folder) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{folder}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "folder", folder);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<PipelineFolderImpl> deserializedResponse = std::make_shared<PipelineFolderImpl>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<PipelineFolderImpl>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<PipelineImpl>
BlueOceanApi::getPipelineFolderPipeline(
    const std::string& organization, const std::string& pipeline, const std::string& folder) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{folder}/pipelines/{pipeline}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "folder", folder);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<PipelineImpl> deserializedResponse = std::make_shared<PipelineImpl>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<PipelineImpl>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<QueueItemImpl>>
BlueOceanApi::getPipelineQueue(
    const std::string& organization, const std::string& pipeline) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/queue";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<QueueItemImpl>> deserializedResponse = std::vector<std::shared_ptr<QueueItemImpl>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<QueueItemImpl>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<PipelineRun>
BlueOceanApi::getPipelineRun(
    const std::string& organization, const std::string& pipeline, const std::string& run) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<PipelineRun> deserializedResponse = std::make_shared<PipelineRun>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<PipelineRun>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::string
BlueOceanApi::getPipelineRunLog(
    const std::string& organization, const std::string& pipeline, const std::string& run, const boost::optional<int32_t>& start, const boost::optional<bool>& download) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/log";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (start) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "start",
            serializeQueryParameterValue(*start));
    }
    if (download) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "download",
            serializeQueryParameterValue(*download));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::string deserializedResponse = "";
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::string>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<PipelineRunNode>
BlueOceanApi::getPipelineRunNode(
    const std::string& organization, const std::string& pipeline, const std::string& run, const std::string& node) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);
    replacePathParameter(path, "node", node);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<PipelineRunNode> deserializedResponse = std::make_shared<PipelineRunNode>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<PipelineRunNode>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<PipelineStepImpl>
BlueOceanApi::getPipelineRunNodeStep(
    const std::string& organization, const std::string& pipeline, const std::string& run, const std::string& node, const std::string& step) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);
    replacePathParameter(path, "node", node);
    replacePathParameter(path, "step", step);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<PipelineStepImpl> deserializedResponse = std::make_shared<PipelineStepImpl>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<PipelineStepImpl>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::string
BlueOceanApi::getPipelineRunNodeStepLog(
    const std::string& organization, const std::string& pipeline, const std::string& run, const std::string& node, const std::string& step) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps/{step}/log";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);
    replacePathParameter(path, "node", node);
    replacePathParameter(path, "step", step);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::string deserializedResponse = "";
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::string>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<PipelineStepImpl>>
BlueOceanApi::getPipelineRunNodeSteps(
    const std::string& organization, const std::string& pipeline, const std::string& run, const std::string& node) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes/{node}/steps";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);
    replacePathParameter(path, "node", node);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<PipelineStepImpl>> deserializedResponse = std::vector<std::shared_ptr<PipelineStepImpl>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<PipelineStepImpl>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<PipelineRunNode>>
BlueOceanApi::getPipelineRunNodes(
    const std::string& organization, const std::string& pipeline, const std::string& run) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/nodes";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<PipelineRunNode>> deserializedResponse = std::vector<std::shared_ptr<PipelineRunNode>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<PipelineRunNode>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<PipelineRun>>
BlueOceanApi::getPipelineRuns(
    const std::string& organization, const std::string& pipeline) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<PipelineRun>> deserializedResponse = std::vector<std::shared_ptr<PipelineRun>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<PipelineRun>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<QueueItemImpl>
BlueOceanApi::postPipelineRuns(
    const std::string& organization, const std::string& pipeline) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("POST",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<QueueItemImpl> deserializedResponse = std::make_shared<QueueItemImpl>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<QueueItemImpl>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<Pipeline>>
BlueOceanApi::getPipelines(
    const std::string& organization) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<Pipeline>> deserializedResponse = std::vector<std::shared_ptr<Pipeline>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<Pipeline>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<GithubScm>
BlueOceanApi::getSCM(
    const std::string& organization, const std::string& scm) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/scm/{scm}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "scm", scm);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<GithubScm> deserializedResponse = std::make_shared<GithubScm>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<GithubScm>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<GithubOrganization>>
BlueOceanApi::getSCMOrganisationRepositories(
    const std::string& organization, const std::string& scm, const std::string& scmOrganisation, const boost::optional<std::string>& credentialId, const boost::optional<int32_t>& pageSize, const boost::optional<int32_t>& pageNumber) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "scm", scm);
    replacePathParameter(path, "scmOrganisation", scmOrganisation);
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (credentialId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "credentialId",
            serializeQueryParameterValue(*credentialId));
    }
    if (pageSize) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "pageSize",
            serializeQueryParameterValue(*pageSize));
    }
    if (pageNumber) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "pageNumber",
            serializeQueryParameterValue(*pageNumber));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<GithubOrganization>> deserializedResponse = std::vector<std::shared_ptr<GithubOrganization>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<GithubOrganization>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<GithubOrganization>>
BlueOceanApi::getSCMOrganisationRepository(
    const std::string& organization, const std::string& scm, const std::string& scmOrganisation, const std::string& repository, const boost::optional<std::string>& credentialId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/scm/{scm}/organizations/{scmOrganisation}/repositories/{repository}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "scm", scm);
    replacePathParameter(path, "scmOrganisation", scmOrganisation);
    replacePathParameter(path, "repository", repository);
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (credentialId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "credentialId",
            serializeQueryParameterValue(*credentialId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<GithubOrganization>> deserializedResponse = std::vector<std::shared_ptr<GithubOrganization>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<GithubOrganization>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<GithubOrganization>>
BlueOceanApi::getSCMOrganisations(
    const std::string& organization, const std::string& scm, const boost::optional<std::string>& credentialId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/scm/{scm}/organizations";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "scm", scm);
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (credentialId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "credentialId",
            serializeQueryParameterValue(*credentialId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<GithubOrganization>> deserializedResponse = std::vector<std::shared_ptr<GithubOrganization>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<GithubOrganization>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<User>
BlueOceanApi::getUser(
    const std::string& organization, const std::string& user) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/users/{user}";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "user", user);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<User> deserializedResponse = std::make_shared<User>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<User>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::vector<std::shared_ptr<FavoriteImpl>>
BlueOceanApi::getUserFavorites(
    const std::string& user) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/users/{user}/favorites";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "user", user);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<FavoriteImpl>> deserializedResponse = std::vector<std::shared_ptr<FavoriteImpl>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<FavoriteImpl>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<User>
BlueOceanApi::getUsers(
    const std::string& organization) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/users";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<User> deserializedResponse = std::make_shared<User>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<User>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<QueueItemImpl>
BlueOceanApi::postPipelineRun(
    const std::string& organization, const std::string& pipeline, const std::string& run) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/replay";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("POST",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<QueueItemImpl> deserializedResponse = std::make_shared<QueueItemImpl>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<QueueItemImpl>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<FavoriteImpl>
BlueOceanApi::putPipelineFavorite(
    const std::string& organization, const std::string& pipeline, const bool& body) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/favorite";
    std::map<std::string, std::string> headers;
    static const std::vector<std::string> contentTypes{ "application/json", };
    std::string requestContentType = selectPreferredContentType(contentTypes);
    headers["Content-Type"] = requestContentType;
    // Body params
    if (isJsonContentType(requestContentType)) {
        serializedRequestBody = boost::json::serialize(toRequestJsonValue(body));
    } else {
        serializedRequestBody = toRawBodyValue(body);
    }
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("PUT",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<FavoriteImpl> deserializedResponse = std::make_shared<FavoriteImpl>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<FavoriteImpl>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::shared_ptr<PipelineRun>
BlueOceanApi::putPipelineRun(
    const std::string& organization, const std::string& pipeline, const std::string& run, const boost::optional<std::string>& blocking, const boost::optional<int32_t>& timeOutInSecs) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/organizations/{organization}/pipelines/{pipeline}/runs/{run}/stop";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "organization", organization);
    replacePathParameter(path, "pipeline", pipeline);
    replacePathParameter(path, "run", run);
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (blocking) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "blocking",
            serializeQueryParameterValue(*blocking));
    }
    if (timeOutInSecs) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "timeOutInSecs",
            serializeQueryParameterValue(*timeOutInSecs));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("PUT",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<PipelineRun> deserializedResponse = std::make_shared<PipelineRun>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<PipelineRun>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::string
BlueOceanApi::search(
    const std::string& q) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/search";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "q",
            serializeQueryParameterValue(q));
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::string deserializedResponse = "";
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::string>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}
std::string
BlueOceanApi::searchClasses(
    const std::string& q) {
    std::string serializedRequestBody;
    std::string path = m_context + "/blue/rest/classes";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "q",
            serializeQueryParameterValue(q));
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::string deserializedResponse = "";
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::string>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw BlueOceanApiException(statusCode, "Authentication failed - incorrect username and/or password");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw BlueOceanApiException(statusCode, "Jenkins requires authentication - please set username and password");
    }
    throw BlueOceanApiException(statusCode, "Unexpected HTTP status code");
}


std::string BlueOceanApi::base64encode(const std::string& str) {
    return base64encodeImpl(str);
}

void BlueOceanApi::setPreferredMediaTypeHeader(std::map<std::string, std::string>& headers, const std::string& headerName, const std::vector<std::string>& contentTypes) {
    const std::string contentType = selectPreferredContentType(contentTypes);
    headers[headerName] = contentType;
}

void BlueOceanApi::handleStdException(const std::exception&) {
    throw;
}

void BlueOceanApi::handleUncaughtException() {
    throw;
}


}  // namespace org
}  // namespace openapitools
}  // namespace client
}  // namespace api
