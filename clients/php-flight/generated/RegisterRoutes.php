<?php

/**
 * Swaggy Jenkins
 * PHP version 8.x
 *
 * @package OpenAPIServer
 * @author  OpenAPI Generator team
 * @link    https://github.com/openapitools/openapi-generator
 */

/**
 * Jenkins API clients generated from Swagger / Open API specification
 * The version of the OpenAPI document: 3.3.1-pre.0
 * Contact: blah+oapicf@cliffano.com
 * Generated by: https://github.com/openapitools/openapi-generator.git
 */

namespace OpenAPIServer;

class RegisterRoutes {

    static public function registerRoutes(\OpenAPIServer\Api\AbstractBaseApi|\OpenAPIServer\Api\AbstractBlueOceanApi|\OpenAPIServer\Api\AbstractRemoteAccessApi $handler): void
    {
        $reflectionClass = new \ReflectionClass($handler);

        if (declaresMethod($reflectionClass, 'getCrumb') && declaresMethod($reflectionClass, 'getCrumbStream')) {
            throw new \Exception('Operation getCrumb cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getCrumb')) {
            \Flight::route('GET /crumbIssuer/api/json', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->getCrumb(
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getCrumbStream')) {
            \Flight::route('GET /crumbIssuer/api/json', function () use ($handler) {
                $r = \Flight::request();
                $handler->getCrumbStream(
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'deletePipelineQueueItem') && declaresMethod($reflectionClass, 'deletePipelineQueueItemStream')) {
            throw new \Exception('Operation deletePipelineQueueItem cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'deletePipelineQueueItem')) {
            \Flight::route('DELETE /blue/rest/organizations/@organization/pipelines/@pipeline/queue/@queue', function (string $organization, string $pipeline, string $queue) use ($handler) {
                $r = \Flight::request();
                $handler->deletePipelineQueueItem(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($queue, 'string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'deletePipelineQueueItemStream')) {
            \Flight::route('DELETE /blue/rest/organizations/@organization/pipelines/@pipeline/queue/@queue', function (string $organization, string $pipeline, string $queue) use ($handler) {
                $r = \Flight::request();
                $handler->deletePipelineQueueItemStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($queue, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'getAuthenticatedUser') && declaresMethod($reflectionClass, 'getAuthenticatedUserStream')) {
            throw new \Exception('Operation getAuthenticatedUser cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getAuthenticatedUser')) {
            \Flight::route('GET /blue/rest/organizations/@organization/user/', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getAuthenticatedUser(
                    parseParam($organization, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getAuthenticatedUserStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/user/', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $handler->getAuthenticatedUserStream(
                    parseParam($organization, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getClasses') && declaresMethod($reflectionClass, 'getClassesStream')) {
            throw new \Exception('Operation getClasses cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getClasses')) {
            \Flight::route('GET /blue/rest/classes/@class', function (string $class) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getClasses(
                    parseParam($class, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getClassesStream')) {
            \Flight::route('GET /blue/rest/classes/@class', function (string $class) use ($handler) {
                $r = \Flight::request();
                $handler->getClassesStream(
                    parseParam($class, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getJsonWebKey') && declaresMethod($reflectionClass, 'getJsonWebKeyStream')) {
            throw new \Exception('Operation getJsonWebKey cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getJsonWebKey')) {
            \Flight::route('GET /jwt-auth/jwks/@key', function (string $key) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getJsonWebKey(
                    parseParam($key, 'int')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getJsonWebKeyStream')) {
            \Flight::route('GET /jwt-auth/jwks/@key', function (string $key) use ($handler) {
                $r = \Flight::request();
                $handler->getJsonWebKeyStream(
                    parseParam($key, 'int')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getJsonWebToken') && declaresMethod($reflectionClass, 'getJsonWebTokenStream')) {
            throw new \Exception('Operation getJsonWebToken cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getJsonWebToken')) {
            \Flight::route('GET /jwt-auth/token', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->getJsonWebToken(
                    parseParam($r->query['expiryTimeInMins'] ?? null, '?int'),
                    parseParam($r->query['maxExpiryTimeInMins'] ?? null, '?int')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getJsonWebTokenStream')) {
            \Flight::route('GET /jwt-auth/token', function () use ($handler) {
                $r = \Flight::request();
                $handler->getJsonWebTokenStream(
                    parseParam($r->query['expiryTimeInMins'] ?? null, '?int'),
                    parseParam($r->query['maxExpiryTimeInMins'] ?? null, '?int')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getOrganisation') && declaresMethod($reflectionClass, 'getOrganisationStream')) {
            throw new \Exception('Operation getOrganisation cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getOrganisation')) {
            \Flight::route('GET /blue/rest/organizations/@organization', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getOrganisation(
                    parseParam($organization, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getOrganisationStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $handler->getOrganisationStream(
                    parseParam($organization, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getOrganisations') && declaresMethod($reflectionClass, 'getOrganisationsStream')) {
            throw new \Exception('Operation getOrganisations cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getOrganisations')) {
            \Flight::route('GET /blue/rest/organizations/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->getOrganisations(
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getOrganisationsStream')) {
            \Flight::route('GET /blue/rest/organizations/', function () use ($handler) {
                $r = \Flight::request();
                $handler->getOrganisationsStream(
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipeline') && declaresMethod($reflectionClass, 'getPipelineStream')) {
            throw new \Exception('Operation getPipeline cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipeline')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipeline(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineActivities') && declaresMethod($reflectionClass, 'getPipelineActivitiesStream')) {
            throw new \Exception('Operation getPipelineActivities cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineActivities')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/activities', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineActivities(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineActivitiesStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/activities', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineActivitiesStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineBranch') && declaresMethod($reflectionClass, 'getPipelineBranchStream')) {
            throw new \Exception('Operation getPipelineBranch cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineBranch')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/branches/@branch/', function (string $organization, string $pipeline, string $branch) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineBranch(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($branch, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineBranchStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/branches/@branch/', function (string $organization, string $pipeline, string $branch) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineBranchStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($branch, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineBranchRun') && declaresMethod($reflectionClass, 'getPipelineBranchRunStream')) {
            throw new \Exception('Operation getPipelineBranchRun cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineBranchRun')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/branches/@branch/runs/@run', function (string $organization, string $pipeline, string $branch, string $run) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineBranchRun(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($branch, 'string'),
                    parseParam($run, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineBranchRunStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/branches/@branch/runs/@run', function (string $organization, string $pipeline, string $branch, string $run) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineBranchRunStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($branch, 'string'),
                    parseParam($run, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineBranches') && declaresMethod($reflectionClass, 'getPipelineBranchesStream')) {
            throw new \Exception('Operation getPipelineBranches cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineBranches')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/branches', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineBranches(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineBranchesStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/branches', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineBranchesStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineFolder') && declaresMethod($reflectionClass, 'getPipelineFolderStream')) {
            throw new \Exception('Operation getPipelineFolder cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineFolder')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@folder/', function (string $organization, string $folder) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineFolder(
                    parseParam($organization, 'string'),
                    parseParam($folder, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineFolderStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@folder/', function (string $organization, string $folder) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineFolderStream(
                    parseParam($organization, 'string'),
                    parseParam($folder, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineFolderPipeline') && declaresMethod($reflectionClass, 'getPipelineFolderPipelineStream')) {
            throw new \Exception('Operation getPipelineFolderPipeline cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineFolderPipeline')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@folder/pipelines/@pipeline', function (string $organization, string $pipeline, string $folder) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineFolderPipeline(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($folder, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineFolderPipelineStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@folder/pipelines/@pipeline', function (string $organization, string $pipeline, string $folder) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineFolderPipelineStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($folder, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineQueue') && declaresMethod($reflectionClass, 'getPipelineQueueStream')) {
            throw new \Exception('Operation getPipelineQueue cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineQueue')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/queue', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineQueue(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineQueueStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/queue', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineQueueStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRun') && declaresMethod($reflectionClass, 'getPipelineRunStream')) {
            throw new \Exception('Operation getPipelineRun cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRun')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRun(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRunLog') && declaresMethod($reflectionClass, 'getPipelineRunLogStream')) {
            throw new \Exception('Operation getPipelineRunLog cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunLog')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/log', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRunLog(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($r->query['start'] ?? null, '?int'),
                    parseParam($r->query['download'] ?? null, '?bool')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunLogStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/log', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunLogStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($r->query['start'] ?? null, '?int'),
                    parseParam($r->query['download'] ?? null, '?bool')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRunNode') && declaresMethod($reflectionClass, 'getPipelineRunNodeStream')) {
            throw new \Exception('Operation getPipelineRunNode cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNode')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node', function (string $organization, string $pipeline, string $run, string $node) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRunNode(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node', function (string $organization, string $pipeline, string $run, string $node) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunNodeStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStep') && declaresMethod($reflectionClass, 'getPipelineRunNodeStepStream')) {
            throw new \Exception('Operation getPipelineRunNodeStep cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStep')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node/steps/@step', function (string $organization, string $pipeline, string $run, string $node, string $step) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRunNodeStep(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string'),
                    parseParam($step, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStepStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node/steps/@step', function (string $organization, string $pipeline, string $run, string $node, string $step) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunNodeStepStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string'),
                    parseParam($step, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStepLog') && declaresMethod($reflectionClass, 'getPipelineRunNodeStepLogStream')) {
            throw new \Exception('Operation getPipelineRunNodeStepLog cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStepLog')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node/steps/@step/log', function (string $organization, string $pipeline, string $run, string $node, string $step) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRunNodeStepLog(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string'),
                    parseParam($step, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStepLogStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node/steps/@step/log', function (string $organization, string $pipeline, string $run, string $node, string $step) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunNodeStepLogStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string'),
                    parseParam($step, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRunNodeSteps') && declaresMethod($reflectionClass, 'getPipelineRunNodeStepsStream')) {
            throw new \Exception('Operation getPipelineRunNodeSteps cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodeSteps')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node/steps', function (string $organization, string $pipeline, string $run, string $node) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRunNodeSteps(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodeStepsStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes/@node/steps', function (string $organization, string $pipeline, string $run, string $node) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunNodeStepsStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($node, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRunNodes') && declaresMethod($reflectionClass, 'getPipelineRunNodesStream')) {
            throw new \Exception('Operation getPipelineRunNodes cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodes')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRunNodes(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunNodesStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/nodes', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunNodesStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelineRuns') && declaresMethod($reflectionClass, 'getPipelineRunsStream')) {
            throw new \Exception('Operation getPipelineRuns cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelineRuns')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelineRuns(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelineRunsStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/@pipeline/runs', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelineRunsStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getPipelines') && declaresMethod($reflectionClass, 'getPipelinesStream')) {
            throw new \Exception('Operation getPipelines cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getPipelines')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getPipelines(
                    parseParam($organization, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getPipelinesStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/pipelines/', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $handler->getPipelinesStream(
                    parseParam($organization, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getSCM') && declaresMethod($reflectionClass, 'getSCMStream')) {
            throw new \Exception('Operation getSCM cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getSCM')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm', function (string $organization, string $scm) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getSCM(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getSCMStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm', function (string $organization, string $scm) use ($handler) {
                $r = \Flight::request();
                $handler->getSCMStream(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getSCMOrganisationRepositories') && declaresMethod($reflectionClass, 'getSCMOrganisationRepositoriesStream')) {
            throw new \Exception('Operation getSCMOrganisationRepositories cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getSCMOrganisationRepositories')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm/organizations/@scmOrganisation/repositories', function (string $organization, string $scm, string $scmOrganisation) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getSCMOrganisationRepositories(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string'),
                    parseParam($scmOrganisation, 'string'),
                    parseParam($r->query['credentialId'] ?? null, '?string'),
                    parseParam($r->query['pageSize'] ?? null, '?int'),
                    parseParam($r->query['pageNumber'] ?? null, '?int')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getSCMOrganisationRepositoriesStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm/organizations/@scmOrganisation/repositories', function (string $organization, string $scm, string $scmOrganisation) use ($handler) {
                $r = \Flight::request();
                $handler->getSCMOrganisationRepositoriesStream(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string'),
                    parseParam($scmOrganisation, 'string'),
                    parseParam($r->query['credentialId'] ?? null, '?string'),
                    parseParam($r->query['pageSize'] ?? null, '?int'),
                    parseParam($r->query['pageNumber'] ?? null, '?int')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getSCMOrganisationRepository') && declaresMethod($reflectionClass, 'getSCMOrganisationRepositoryStream')) {
            throw new \Exception('Operation getSCMOrganisationRepository cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getSCMOrganisationRepository')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm/organizations/@scmOrganisation/repositories/@repository', function (string $organization, string $scm, string $scmOrganisation, string $repository) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getSCMOrganisationRepository(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string'),
                    parseParam($scmOrganisation, 'string'),
                    parseParam($repository, 'string'),
                    parseParam($r->query['credentialId'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getSCMOrganisationRepositoryStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm/organizations/@scmOrganisation/repositories/@repository', function (string $organization, string $scm, string $scmOrganisation, string $repository) use ($handler) {
                $r = \Flight::request();
                $handler->getSCMOrganisationRepositoryStream(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string'),
                    parseParam($scmOrganisation, 'string'),
                    parseParam($repository, 'string'),
                    parseParam($r->query['credentialId'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getSCMOrganisations') && declaresMethod($reflectionClass, 'getSCMOrganisationsStream')) {
            throw new \Exception('Operation getSCMOrganisations cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getSCMOrganisations')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm/organizations', function (string $organization, string $scm) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getSCMOrganisations(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string'),
                    parseParam($r->query['credentialId'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getSCMOrganisationsStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/scm/@scm/organizations', function (string $organization, string $scm) use ($handler) {
                $r = \Flight::request();
                $handler->getSCMOrganisationsStream(
                    parseParam($organization, 'string'),
                    parseParam($scm, 'string'),
                    parseParam($r->query['credentialId'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getUser') && declaresMethod($reflectionClass, 'getUserStream')) {
            throw new \Exception('Operation getUser cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getUser')) {
            \Flight::route('GET /blue/rest/organizations/@organization/users/@user', function (string $organization, string $user) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getUser(
                    parseParam($organization, 'string'),
                    parseParam($user, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getUserStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/users/@user', function (string $organization, string $user) use ($handler) {
                $r = \Flight::request();
                $handler->getUserStream(
                    parseParam($organization, 'string'),
                    parseParam($user, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getUserFavorites') && declaresMethod($reflectionClass, 'getUserFavoritesStream')) {
            throw new \Exception('Operation getUserFavorites cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getUserFavorites')) {
            \Flight::route('GET /blue/rest/users/@user/favorites', function (string $user) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getUserFavorites(
                    parseParam($user, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getUserFavoritesStream')) {
            \Flight::route('GET /blue/rest/users/@user/favorites', function (string $user) use ($handler) {
                $r = \Flight::request();
                $handler->getUserFavoritesStream(
                    parseParam($user, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getUsers') && declaresMethod($reflectionClass, 'getUsersStream')) {
            throw new \Exception('Operation getUsers cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getUsers')) {
            \Flight::route('GET /blue/rest/organizations/@organization/users/', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getUsers(
                    parseParam($organization, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getUsersStream')) {
            \Flight::route('GET /blue/rest/organizations/@organization/users/', function (string $organization) use ($handler) {
                $r = \Flight::request();
                $handler->getUsersStream(
                    parseParam($organization, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'postPipelineRun') && declaresMethod($reflectionClass, 'postPipelineRunStream')) {
            throw new \Exception('Operation postPipelineRun cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postPipelineRun')) {
            \Flight::route('POST /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/replay', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $result = $handler->postPipelineRun(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'postPipelineRunStream')) {
            \Flight::route('POST /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/replay', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $handler->postPipelineRunStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'postPipelineRuns') && declaresMethod($reflectionClass, 'postPipelineRunsStream')) {
            throw new \Exception('Operation postPipelineRuns cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postPipelineRuns')) {
            \Flight::route('POST /blue/rest/organizations/@organization/pipelines/@pipeline/runs', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $result = $handler->postPipelineRuns(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'postPipelineRunsStream')) {
            \Flight::route('POST /blue/rest/organizations/@organization/pipelines/@pipeline/runs', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $handler->postPipelineRunsStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'putPipelineFavorite') && declaresMethod($reflectionClass, 'putPipelineFavoriteStream')) {
            throw new \Exception('Operation putPipelineFavorite cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'putPipelineFavorite')) {
            \Flight::route('PUT /blue/rest/organizations/@organization/pipelines/@pipeline/favorite', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $result = $handler->putPipelineFavorite(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam(json_decode($r->getBody(), true), 'bool')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'putPipelineFavoriteStream')) {
            \Flight::route('PUT /blue/rest/organizations/@organization/pipelines/@pipeline/favorite', function (string $organization, string $pipeline) use ($handler) {
                $r = \Flight::request();
                $handler->putPipelineFavoriteStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam(json_decode($r->getBody(), true), 'bool')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'putPipelineRun') && declaresMethod($reflectionClass, 'putPipelineRunStream')) {
            throw new \Exception('Operation putPipelineRun cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'putPipelineRun')) {
            \Flight::route('PUT /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/stop', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $result = $handler->putPipelineRun(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($r->query['blocking'] ?? null, '?string'),
                    parseParam($r->query['timeOutInSecs'] ?? null, '?int')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'putPipelineRunStream')) {
            \Flight::route('PUT /blue/rest/organizations/@organization/pipelines/@pipeline/runs/@run/stop', function (string $organization, string $pipeline, string $run) use ($handler) {
                $r = \Flight::request();
                $handler->putPipelineRunStream(
                    parseParam($organization, 'string'),
                    parseParam($pipeline, 'string'),
                    parseParam($run, 'string'),
                    parseParam($r->query['blocking'] ?? null, '?string'),
                    parseParam($r->query['timeOutInSecs'] ?? null, '?int')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'search') && declaresMethod($reflectionClass, 'searchStream')) {
            throw new \Exception('Operation search cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'search')) {
            \Flight::route('GET /blue/rest/search/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->search(
                    parseParam($r->query['q'] ?? null, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'searchStream')) {
            \Flight::route('GET /blue/rest/search/', function () use ($handler) {
                $r = \Flight::request();
                $handler->searchStream(
                    parseParam($r->query['q'] ?? null, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'searchClasses') && declaresMethod($reflectionClass, 'searchClassesStream')) {
            throw new \Exception('Operation searchClasses cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'searchClasses')) {
            \Flight::route('GET /blue/rest/classes/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->searchClasses(
                    parseParam($r->query['q'] ?? null, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'searchClassesStream')) {
            \Flight::route('GET /blue/rest/classes/', function () use ($handler) {
                $r = \Flight::request();
                $handler->searchClassesStream(
                    parseParam($r->query['q'] ?? null, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getComputer') && declaresMethod($reflectionClass, 'getComputerStream')) {
            throw new \Exception('Operation getComputer cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getComputer')) {
            \Flight::route('GET /computer/api/json', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->getComputer(
                    parseParam($r->query['depth'] ?? null, 'int')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getComputerStream')) {
            \Flight::route('GET /computer/api/json', function () use ($handler) {
                $r = \Flight::request();
                $handler->getComputerStream(
                    parseParam($r->query['depth'] ?? null, 'int')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getJenkins') && declaresMethod($reflectionClass, 'getJenkinsStream')) {
            throw new \Exception('Operation getJenkins cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getJenkins')) {
            \Flight::route('GET /api/json', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->getJenkins(
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getJenkinsStream')) {
            \Flight::route('GET /api/json', function () use ($handler) {
                $r = \Flight::request();
                $handler->getJenkinsStream(
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getJob') && declaresMethod($reflectionClass, 'getJobStream')) {
            throw new \Exception('Operation getJob cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getJob')) {
            \Flight::route('GET /job/@name/api/json', function (string $name) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getJob(
                    parseParam($name, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getJobStream')) {
            \Flight::route('GET /job/@name/api/json', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->getJobStream(
                    parseParam($name, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getJobConfig') && declaresMethod($reflectionClass, 'getJobConfigStream')) {
            throw new \Exception('Operation getJobConfig cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getJobConfig')) {
            \Flight::route('GET /job/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getJobConfig(
                    parseParam($name, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getJobConfigStream')) {
            \Flight::route('GET /job/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->getJobConfigStream(
                    parseParam($name, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'text/xml']);
        }

        if (declaresMethod($reflectionClass, 'getJobLastBuild') && declaresMethod($reflectionClass, 'getJobLastBuildStream')) {
            throw new \Exception('Operation getJobLastBuild cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getJobLastBuild')) {
            \Flight::route('GET /job/@name/lastBuild/api/json', function (string $name) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getJobLastBuild(
                    parseParam($name, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getJobLastBuildStream')) {
            \Flight::route('GET /job/@name/lastBuild/api/json', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->getJobLastBuildStream(
                    parseParam($name, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getJobProgressiveText') && declaresMethod($reflectionClass, 'getJobProgressiveTextStream')) {
            throw new \Exception('Operation getJobProgressiveText cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getJobProgressiveText')) {
            \Flight::route('GET /job/@name/@number/logText/progressiveText', function (string $name, string $number) use ($handler) {
                $r = \Flight::request();
                $handler->getJobProgressiveText(
                    parseParam($name, 'string'),
                    parseParam($number, 'string'),
                    parseParam($r->query['start'] ?? null, 'string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'getJobProgressiveTextStream')) {
            \Flight::route('GET /job/@name/@number/logText/progressiveText', function (string $name, string $number) use ($handler) {
                $r = \Flight::request();
                $handler->getJobProgressiveTextStream(
                    parseParam($name, 'string'),
                    parseParam($number, 'string'),
                    parseParam($r->query['start'] ?? null, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'getQueue') && declaresMethod($reflectionClass, 'getQueueStream')) {
            throw new \Exception('Operation getQueue cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getQueue')) {
            \Flight::route('GET /queue/api/json', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->getQueue(
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getQueueStream')) {
            \Flight::route('GET /queue/api/json', function () use ($handler) {
                $r = \Flight::request();
                $handler->getQueueStream(
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getQueueItem') && declaresMethod($reflectionClass, 'getQueueItemStream')) {
            throw new \Exception('Operation getQueueItem cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getQueueItem')) {
            \Flight::route('GET /queue/item/@number/api/json', function (string $number) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getQueueItem(
                    parseParam($number, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getQueueItemStream')) {
            \Flight::route('GET /queue/item/@number/api/json', function (string $number) use ($handler) {
                $r = \Flight::request();
                $handler->getQueueItemStream(
                    parseParam($number, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getView') && declaresMethod($reflectionClass, 'getViewStream')) {
            throw new \Exception('Operation getView cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getView')) {
            \Flight::route('GET /view/@name/api/json', function (string $name) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getView(
                    parseParam($name, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getViewStream')) {
            \Flight::route('GET /view/@name/api/json', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->getViewStream(
                    parseParam($name, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'getViewConfig') && declaresMethod($reflectionClass, 'getViewConfigStream')) {
            throw new \Exception('Operation getViewConfig cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'getViewConfig')) {
            \Flight::route('GET /view/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $result = $handler->getViewConfig(
                    parseParam($name, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'getViewConfigStream')) {
            \Flight::route('GET /view/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->getViewConfigStream(
                    parseParam($name, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'text/xml']);
        }

        if (declaresMethod($reflectionClass, 'headJenkins') && declaresMethod($reflectionClass, 'headJenkinsStream')) {
            throw new \Exception('Operation headJenkins cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'headJenkins')) {
            \Flight::route('HEAD /api/json', function () use ($handler) {
                $r = \Flight::request();
                $handler->headJenkins(
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'headJenkinsStream')) {
            \Flight::route('HEAD /api/json', function () use ($handler) {
                $r = \Flight::request();
                $handler->headJenkinsStream(
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postCreateItem') && declaresMethod($reflectionClass, 'postCreateItemStream')) {
            throw new \Exception('Operation postCreateItem cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postCreateItem')) {
            \Flight::route('POST /createItem', function () use ($handler) {
                $r = \Flight::request();
                $handler->postCreateItem(
                    parseParam($r->query['name'] ?? null, 'string'),
                    parseParam($r->query['from'] ?? null, '?string'),
                    parseParam($r->query['mode'] ?? null, '?string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string'),
                    parseParam($r->getHeader('Content-Type'), '?string'),
                    parseParam(json_decode($r->getBody(), true), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postCreateItemStream')) {
            \Flight::route('POST /createItem', function () use ($handler) {
                $r = \Flight::request();
                $handler->postCreateItemStream(
                    parseParam($r->query['name'] ?? null, 'string'),
                    parseParam($r->query['from'] ?? null, '?string'),
                    parseParam($r->query['mode'] ?? null, '?string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string'),
                    parseParam($r->getHeader('Content-Type'), '?string'),
                    parseParam(json_decode($r->getBody(), true), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postCreateView') && declaresMethod($reflectionClass, 'postCreateViewStream')) {
            throw new \Exception('Operation postCreateView cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postCreateView')) {
            \Flight::route('POST /createView', function () use ($handler) {
                $r = \Flight::request();
                $handler->postCreateView(
                    parseParam($r->query['name'] ?? null, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string'),
                    parseParam($r->getHeader('Content-Type'), '?string'),
                    parseParam(json_decode($r->getBody(), true), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postCreateViewStream')) {
            \Flight::route('POST /createView', function () use ($handler) {
                $r = \Flight::request();
                $handler->postCreateViewStream(
                    parseParam($r->query['name'] ?? null, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string'),
                    parseParam($r->getHeader('Content-Type'), '?string'),
                    parseParam(json_decode($r->getBody(), true), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postJobBuild') && declaresMethod($reflectionClass, 'postJobBuildStream')) {
            throw new \Exception('Operation postJobBuild cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postJobBuild')) {
            \Flight::route('POST /job/@name/build', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobBuild(
                    parseParam($name, 'string'),
                    parseParam($r->query['json'] ?? null, 'string'),
                    parseParam($r->query['token'] ?? null, '?string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postJobBuildStream')) {
            \Flight::route('POST /job/@name/build', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobBuildStream(
                    parseParam($name, 'string'),
                    parseParam($r->query['json'] ?? null, 'string'),
                    parseParam($r->query['token'] ?? null, '?string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postJobConfig') && declaresMethod($reflectionClass, 'postJobConfigStream')) {
            throw new \Exception('Operation postJobConfig cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postJobConfig')) {
            \Flight::route('POST /job/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobConfig(
                    parseParam($name, 'string'),
                    parseParam(json_decode($r->getBody(), true), 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postJobConfigStream')) {
            \Flight::route('POST /job/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobConfigStream(
                    parseParam($name, 'string'),
                    parseParam(json_decode($r->getBody(), true), 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postJobDelete') && declaresMethod($reflectionClass, 'postJobDeleteStream')) {
            throw new \Exception('Operation postJobDelete cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postJobDelete')) {
            \Flight::route('POST /job/@name/doDelete', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobDelete(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postJobDeleteStream')) {
            \Flight::route('POST /job/@name/doDelete', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobDeleteStream(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postJobDisable') && declaresMethod($reflectionClass, 'postJobDisableStream')) {
            throw new \Exception('Operation postJobDisable cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postJobDisable')) {
            \Flight::route('POST /job/@name/disable', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobDisable(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postJobDisableStream')) {
            \Flight::route('POST /job/@name/disable', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobDisableStream(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postJobEnable') && declaresMethod($reflectionClass, 'postJobEnableStream')) {
            throw new \Exception('Operation postJobEnable cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postJobEnable')) {
            \Flight::route('POST /job/@name/enable', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobEnable(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postJobEnableStream')) {
            \Flight::route('POST /job/@name/enable', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobEnableStream(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postJobLastBuildStop') && declaresMethod($reflectionClass, 'postJobLastBuildStopStream')) {
            throw new \Exception('Operation postJobLastBuildStop cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postJobLastBuildStop')) {
            \Flight::route('POST /job/@name/lastBuild/stop', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobLastBuildStop(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postJobLastBuildStopStream')) {
            \Flight::route('POST /job/@name/lastBuild/stop', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postJobLastBuildStopStream(
                    parseParam($name, 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

        if (declaresMethod($reflectionClass, 'postViewConfig') && declaresMethod($reflectionClass, 'postViewConfigStream')) {
            throw new \Exception('Operation postViewConfig cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'postViewConfig')) {
            \Flight::route('POST /view/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postViewConfig(
                    parseParam($name, 'string'),
                    parseParam(json_decode($r->getBody(), true), 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                \Flight::halt(200);
            });
        }
        if (declaresMethod($reflectionClass, 'postViewConfigStream')) {
            \Flight::route('POST /view/@name/config.xml', function (string $name) use ($handler) {
                $r = \Flight::request();
                $handler->postViewConfigStream(
                    parseParam($name, 'string'),
                    parseParam(json_decode($r->getBody(), true), 'string'),
                    parseParam($r->getHeader('Jenkins-Crumb'), '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200]);
        }

    }
}


function parseParam(mixed $param, string $type)
{
    $nonNullType = str_replace('?', '', $type);
    if ($param === null) {
        return null;
    } elseif ($nonNullType === 'int') {
        return intval($param);
    } elseif ($nonNullType === 'float') {
        return floatval($param);
    } elseif ($nonNullType === 'bool') {
        return filter_var($param, FILTER_VALIDATE_BOOLEAN);
    } elseif (str_ends_with($type, '[]')) {
        return array_map(fn($el) => parseParam($el, substr($type, 0, -2)), $param);
    } elseif (str_starts_with($nonNullType, '\\OpenAPIServer\\Model')) {
        if (enum_exists($nonNullType)) {
            return $nonNullType::tryFrom($param);
        }
        return $nonNullType::fromArray($param);
    } else {
        return $param;
    }
}

function declaresMethod(\ReflectionClass $reflectionClass, string $methodName): bool
{
    return $reflectionClass->hasMethod($methodName) && $reflectionClass->getMethod($methodName)->getDeclaringClass()->getName() === $reflectionClass->getName();
}
