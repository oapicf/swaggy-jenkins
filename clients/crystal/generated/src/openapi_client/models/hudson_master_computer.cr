# #Swaggy Jenkins
#
##Jenkins API clients generated from Swagger / Open API specification
#
#The version of the OpenAPI document: 3.2.1-pre.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class HudsonMasterComputer
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "_class", type: String?, nillable: true, emit_null: false)]
    property _class : String?

    @[JSON::Field(key: "displayName", type: String?, nillable: true, emit_null: false)]
    property display_name : String?

    @[JSON::Field(key: "executors", type: Array(HudsonMasterComputerexecutors)?, nillable: true, emit_null: false)]
    property executors : Array(HudsonMasterComputerexecutors)?

    @[JSON::Field(key: "icon", type: String?, nillable: true, emit_null: false)]
    property icon : String?

    @[JSON::Field(key: "iconClassName", type: String?, nillable: true, emit_null: false)]
    property icon_class_name : String?

    @[JSON::Field(key: "idle", type: Bool?, nillable: true, emit_null: false)]
    property idle : Bool?

    @[JSON::Field(key: "jnlpAgent", type: Bool?, nillable: true, emit_null: false)]
    property jnlp_agent : Bool?

    @[JSON::Field(key: "launchSupported", type: Bool?, nillable: true, emit_null: false)]
    property launch_supported : Bool?

    @[JSON::Field(key: "loadStatistics", type: Label1?, nillable: true, emit_null: false)]
    property load_statistics : Label1?

    @[JSON::Field(key: "manualLaunchAllowed", type: Bool?, nillable: true, emit_null: false)]
    property manual_launch_allowed : Bool?

    @[JSON::Field(key: "monitorData", type: HudsonMasterComputermonitorData?, nillable: true, emit_null: false)]
    property monitor_data : HudsonMasterComputermonitorData?

    @[JSON::Field(key: "numExecutors", type: Int32?, nillable: true, emit_null: false)]
    property num_executors : Int32?

    @[JSON::Field(key: "offline", type: Bool?, nillable: true, emit_null: false)]
    property offline : Bool?

    @[JSON::Field(key: "offlineCause", type: String?, nillable: true, emit_null: false)]
    property offline_cause : String?

    @[JSON::Field(key: "offlineCauseReason", type: String?, nillable: true, emit_null: false)]
    property offline_cause_reason : String?

    @[JSON::Field(key: "temporarilyOffline", type: Bool?, nillable: true, emit_null: false)]
    property temporarily_offline : Bool?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@_class : String? = nil, @display_name : String? = nil, @executors : Array(HudsonMasterComputerexecutors)? = nil, @icon : String? = nil, @icon_class_name : String? = nil, @idle : Bool? = nil, @jnlp_agent : Bool? = nil, @launch_supported : Bool? = nil, @load_statistics : Label1? = nil, @manual_launch_allowed : Bool? = nil, @monitor_data : HudsonMasterComputermonitorData? = nil, @num_executors : Int32? = nil, @offline : Bool? = nil, @offline_cause : String? = nil, @offline_cause_reason : String? = nil, @temporarily_offline : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          _class == other._class &&
          display_name == other.display_name &&
          executors == other.executors &&
          icon == other.icon &&
          icon_class_name == other.icon_class_name &&
          idle == other.idle &&
          jnlp_agent == other.jnlp_agent &&
          launch_supported == other.launch_supported &&
          load_statistics == other.load_statistics &&
          manual_launch_allowed == other.manual_launch_allowed &&
          monitor_data == other.monitor_data &&
          num_executors == other.num_executors &&
          offline == other.offline &&
          offline_cause == other.offline_cause &&
          offline_cause_reason == other.offline_cause_reason &&
          temporarily_offline == other.temporarily_offline
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [_class, display_name, executors, icon, icon_class_name, idle, jnlp_agent, launch_supported, load_statistics, manual_launch_allowed, monitor_data, num_executors, offline, offline_cause, offline_cause_reason, temporarily_offline].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["_class"] = _to_h(_class)
      hash["displayName"] = _to_h(display_name)
      hash["executors"] = _to_h(executors)
      hash["icon"] = _to_h(icon)
      hash["iconClassName"] = _to_h(icon_class_name)
      hash["idle"] = _to_h(idle)
      hash["jnlpAgent"] = _to_h(jnlp_agent)
      hash["launchSupported"] = _to_h(launch_supported)
      hash["loadStatistics"] = _to_h(load_statistics)
      hash["manualLaunchAllowed"] = _to_h(manual_launch_allowed)
      hash["monitorData"] = _to_h(monitor_data)
      hash["numExecutors"] = _to_h(num_executors)
      hash["offline"] = _to_h(offline)
      hash["offlineCause"] = _to_h(offline_cause)
      hash["offlineCauseReason"] = _to_h(offline_cause_reason)
      hash["temporarilyOffline"] = _to_h(temporarily_offline)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
