# #Swaggy Jenkins
#
##Jenkins API clients generated from Swagger / Open API specification
#
#The version of the OpenAPI document: 3.2.1-pre.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class Hudson
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "_class", type: String?, nillable: true, emit_null: false)]
    property _class : String?

    @[JSON::Field(key: "assignedLabels", type: Array(HudsonassignedLabels)?, nillable: true, emit_null: false)]
    property assigned_labels : Array(HudsonassignedLabels)?

    @[JSON::Field(key: "mode", type: String?, nillable: true, emit_null: false)]
    property mode : String?

    @[JSON::Field(key: "nodeDescription", type: String?, nillable: true, emit_null: false)]
    property node_description : String?

    @[JSON::Field(key: "nodeName", type: String?, nillable: true, emit_null: false)]
    property node_name : String?

    @[JSON::Field(key: "numExecutors", type: Int32?, nillable: true, emit_null: false)]
    property num_executors : Int32?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "jobs", type: Array(FreeStyleProject)?, nillable: true, emit_null: false)]
    property jobs : Array(FreeStyleProject)?

    @[JSON::Field(key: "primaryView", type: AllView?, nillable: true, emit_null: false)]
    property primary_view : AllView?

    @[JSON::Field(key: "quietingDown", type: Bool?, nillable: true, emit_null: false)]
    property quieting_down : Bool?

    @[JSON::Field(key: "slaveAgentPort", type: Int32?, nillable: true, emit_null: false)]
    property slave_agent_port : Int32?

    @[JSON::Field(key: "unlabeledLoad", type: UnlabeledLoadStatistics?, nillable: true, emit_null: false)]
    property unlabeled_load : UnlabeledLoadStatistics?

    @[JSON::Field(key: "useCrumbs", type: Bool?, nillable: true, emit_null: false)]
    property use_crumbs : Bool?

    @[JSON::Field(key: "useSecurity", type: Bool?, nillable: true, emit_null: false)]
    property use_security : Bool?

    @[JSON::Field(key: "views", type: Array(AllView)?, nillable: true, emit_null: false)]
    property views : Array(AllView)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@_class : String? = nil, @assigned_labels : Array(HudsonassignedLabels)? = nil, @mode : String? = nil, @node_description : String? = nil, @node_name : String? = nil, @num_executors : Int32? = nil, @description : String? = nil, @jobs : Array(FreeStyleProject)? = nil, @primary_view : AllView? = nil, @quieting_down : Bool? = nil, @slave_agent_port : Int32? = nil, @unlabeled_load : UnlabeledLoadStatistics? = nil, @use_crumbs : Bool? = nil, @use_security : Bool? = nil, @views : Array(AllView)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          _class == other._class &&
          assigned_labels == other.assigned_labels &&
          mode == other.mode &&
          node_description == other.node_description &&
          node_name == other.node_name &&
          num_executors == other.num_executors &&
          description == other.description &&
          jobs == other.jobs &&
          primary_view == other.primary_view &&
          quieting_down == other.quieting_down &&
          slave_agent_port == other.slave_agent_port &&
          unlabeled_load == other.unlabeled_load &&
          use_crumbs == other.use_crumbs &&
          use_security == other.use_security &&
          views == other.views
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [_class, assigned_labels, mode, node_description, node_name, num_executors, description, jobs, primary_view, quieting_down, slave_agent_port, unlabeled_load, use_crumbs, use_security, views].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["_class"] = _to_h(_class)
      hash["assignedLabels"] = _to_h(assigned_labels)
      hash["mode"] = _to_h(mode)
      hash["nodeDescription"] = _to_h(node_description)
      hash["nodeName"] = _to_h(node_name)
      hash["numExecutors"] = _to_h(num_executors)
      hash["description"] = _to_h(description)
      hash["jobs"] = _to_h(jobs)
      hash["primaryView"] = _to_h(primary_view)
      hash["quietingDown"] = _to_h(quieting_down)
      hash["slaveAgentPort"] = _to_h(slave_agent_port)
      hash["unlabeledLoad"] = _to_h(unlabeled_load)
      hash["useCrumbs"] = _to_h(use_crumbs)
      hash["useSecurity"] = _to_h(use_security)
      hash["views"] = _to_h(views)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
